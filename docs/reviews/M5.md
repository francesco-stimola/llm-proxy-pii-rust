# M5 — Integration & performance testing · review record

The full record of every M5 review finding: repro, mechanism, `file:line`, the **fix**, and the
**test** that would have caught it. The ledger (id · title · severity · status) lives in
[`../ROADMAP.md`](../ROADMAP.md#m5); a finding is never copied between the two.

---

## Review 1 — M5 integration & performance (2026-07-14)

Reviewed `cd0395c` (integration tests) · `df62387` (perf harness) · `12c9ea8` (NER chunking) ·
`442144c` (README, CI/release, manual runbook).

### What I verified independently

**Build & suite.** `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` and
`cargo clippy --all-targets --features onnx -- -D warnings` all clean; `cargo test` **132 passed**
(+1 `#[ignore]`d), `cargo test --features onnx` **144 passed** (+4 `#[ignore]`d). Zero warnings on
both feature sets. The ROADMAP's counts are accurate.

**The chunking bug is real — reproduced from the pre-fix commit.** I built a detached worktree at
`df62387` (the commit *before* the fix), dropped in the new `tests/ner_perf.rs`, and ran it against
the real XLM-R int8 model. It fails exactly as claimed, and abruptly:

```text
reps=   16 (944 chars): 94.1821ms, 48/48 entities found
thread 'onnx_ner_latency_and_recall_across_field_sizes' panicked at tests\ner_perf.rs:94:35:
reps=64: NER call failed: onnx-ner detector failed: ONNX run: Non-zero status code returned
while running Expand node. Name:'/roberta/Expand' Status Message: invalid expand shape
```

So the failure mode really is an **outright error at ~3.8 KB of prose**, not a slowdown — and under
`NER_REQUIRED` that is a hard 400 on every such request. The diagnosis in ARCHITECTURE / DEVLOG /
ROADMAP is correct, including the correction of the *suspected* cause (quadratic self-attention) to
the actual one (position-embedding overflow). This is the right kind of finding: the measurement
was allowed to contradict the hypothesis.

**The fix works, and I re-measured it.** On `HEAD` the same test passes with linear scaling —
**460 ms / 1.96 s / 7.77 s** at 64×/256×/1024× (the builder recorded 448 ms / 2.07 s / 7.53 s;
same machine class, debug profile). Entity counts `192/192`, `771/768`, `3084/3072` — reproducing
the *over*-count the docs honestly flag.

**Drove the real binary end-to-end** (`--features onnx`, `NER_REQUIRED=1`, `RUST_LOG=…=trace`,
mock upstream, a **3852-char** field — well past the pre-fix cliff):

| | result |
|---|---|
| upstream saw `bob@test.com` / `4111111111111111` / `Mario Rossi` / `Giulia Bianchi` | **no** (all four) |
| upstream saw `[EMAIL_1]` `[CARD_1]` `[PERSON_1]` `[ORG_1]` `[LOCATION_1]` + the augmentation message | **yes** (all) |
| client got all four raw values restored; any `[KIND_N]` left unresolved | **yes** / **none** |
| raw PII anywhere in the `trace` logs | **none** (placeholders only) |

`[PERSON_1]`/`[ORG_1]`/`[LOCATION_1]` reaching the upstream on a 3852-char field *is* the proof that
chunking works in production, not just in a test: pre-fix, NER on that field returned nothing at all
(or 400 under `NER_REQUIRED`).

**What I tried to break and couldn't.**

- *Can the chunk slice panic on a multi-byte char?* I copied `chunk_char_ranges` verbatim into an
  off-repo probe and ran it with the **real** XLM-R tokenizer over 13 adversarial inputs — Chinese,
  Japanese, Cyrillic, emoji + flag sequences, combining marks, zalgo, mixed-script glue, a
  20 000-char single token, base64 runs, whitespace floods. **Every** window boundary landed on a
  `char` boundary; `&input[char_start..char_end]` never became unsliceable. (The *shape* of the risk
  is still real — see [M5-R3](#m5-r3) — but I could not produce it.)
- *Can chunking drop or duplicate an entity at a window boundary?* No, and for a reason worth
  writing down (I have — see the promotion note below). Windows are 480 tokens with a 448-token
  stride, so any entity ≤ 32 tokens is whole in at least one window. A *truncated* fragment from a
  neighbouring window (`Mil` vs `Milan`) survives `infer_chunked`'s `dedup()` (it only removes
  **exact** duplicates), but `overlap::resolve_overlaps` then cleans it up: all three NER kinds share
  `priority() == 0`, so its NER phase tiebreaks on **span length, descending** — the whole entity is
  taken first and the fragment is dropped as overlapping. The `>100%` entity counts in `ner_perf`
  are bare-detector output; production never sees them.
- *Does the mask→re-detect **fixpoint** still converge now that large fields actually reach the
  NER?* This is the one that worried me: ARCHITECTURE argues convergence from "a placeholder is
  **inert**", but that argument is about **recognizers** (regex), and an ML model is under no such
  constraint. I ran the real detector over 2 520 bytes of placeholder-only text
  (`Contact [PERSON_1] at [ORG_1] in [LOCATION_1] about [EMAIL_1]. ` ×40, large enough to be
  chunked): **0 entities tagged**. Full-hybrid `mask_all` converges on 412 B / 6.6 KB / 26 KB fields
  with no raw PII in the masked text. It holds — **empirically**, which is not the same as by
  construction. See [M5-R4](#m5-r4).
- *Are the release binaries self-contained?* Yes — `ort`'s `download-binaries` links ONNX Runtime
  **statically**: there is no `onnxruntime.dll` anywhere under `target/`, and the built `.exe` ran
  standalone. `release.yml` shipping a bare binary per target is therefore correct.

**No leak, no fail-open, no over-mask regression found.** Chunking is confined to the NER
(`OnnxNerDetector::infer`); structured PII is still detected over the whole field, unchunked, by the
fail-closed layer. The M5 findings below are one stale doc, three hardening/invariant items, and two
accuracy nits — nothing that forwards PII.

---

<a id="m5-r1"></a>
### M5-R1 — TESTING.md still says the NER is unchunked and quadratic — the exact claim M5 disproved *and* fixed

**Severity: docs (medium — it is in the file that governs the guards).**

`docs/TESTING.md:294-297`, closing the *Algorithmic complexity* section:

> **What is NOT covered here (stated so nobody reads more into it).** These guards bound the
> **structured** (default-build) path. The optional `onnx` NER feeds the whole field as **one
> sequence** with no chunking, so its self-attention is quadratic in field size — a separate,
> opt-in cost, to be **measured** by M5's perf harness (PERF-01) before NER is recommended for
> large bodies.

Every clause of this is now false:

1. the NER **does** chunk (`src/pii/onnx.rs::infer_chunked`, `12c9ea8`);
2. "its self-attention is quadratic in field size" is the hypothesis M5 **measured and refuted** —
   the real behavior was an *outright `Expand`-op error* past the position-embedding limit, which is
   the whole point of the finding;
3. "to be measured by M5's perf harness" is a **stale open point** — PERF-01 ran, and this is its
   result.

The equivalent block in `ARCHITECTURE.md:109-112` *was* updated (it now says "chunked and measured
linear (M5, PERF-01)"). TESTING.md's was not. A reader of TESTING.md — the file a builder opens
before touching a guard — is told the opposite of what shipped, and is told the disproved cause as
fact. This is precisely the "no stale open points" bar.

**Fix.** Replace the block with what M5 actually established: the guards bound the **structured**
path; the `onnx` NER is chunked (`MAX_SEQUENCE_TOKENS` / `CHUNK_OVERLAP_TOKENS`) and measured linear
in field size (PERF-01: 460 ms / 1.96 s / 7.77 s at 64×/256×/1024×), and it is a **recall**
mechanism only — never leak-relevant, because structured PII is detected over the whole field,
unchunked. Point at `ARCHITECTURE.md` → *NER chunking (M5, PERF-01)* rather than restating it.

**Test.** None possible — this is prose. The guard is the review itself; the reason it slipped is
that the same claim lived in two files and only one was updated. Keeping the *statement* in
ARCHITECTURE and a **pointer** in TESTING removes the duplicate that made the drift possible.

> **Closed 2026-07-14.** `TESTING.md`'s *What is NOT covered here* box now states what M5 actually
> established (the NER is **chunked**, measured linear, and a **recall** mechanism only, never
> leak-relevant) and **points at** `ARCHITECTURE.md` → *NER chunking (M5, PERF-01)* instead of
> restating the mechanism. The duplicate that allowed the drift is gone, and the box says so
> explicitly: **one home for a fact** is the project's rule for review findings, and it applies to
> design claims identically.

---

<a id="m5-r2"></a>
### M5-R2 — `MAX_SEQUENCE_TOKENS` does not bound what actually reaches the model

**Severity: hardening / availability (low-medium).**

`src/pii/onnx.rs:43-47`:

```rust
/// Upper bound on tokens (special tokens included) fed to the model in one call.
const MAX_SEQUENCE_TOKENS: usize = 480;
```

It isn't. `chunk_char_ranges` picks the window in the coordinates of the **whole field's**
tokenization, but `infer_chunked` then **re-tokenizes each window from its own text**
(`onnx.rs:161-164`) — correctly, because a middle window needs its own `<s>…</s>` framing. The
re-tokenized chunk is a *different* tokenization: it adds 2 special tokens and drifts at the two cut
edges. Nothing re-checks the result before `run_and_decode` hands it to the session.

Measured with the real XLM-R tokenizer (off-repo probe, verbatim copy of `chunk_char_ranges`, 13
inputs incl. CJK / combining marks / no-space runs): **every** chunk re-tokenizes to **481–483
tokens** — i.e. *always* over the 480 the constant claims to enforce.

That is fine today, but the margin is thinner than it looks. XLM-R's `config.json` declares
`max_position_embeddings: 514`, and RoBERTa-family position ids start at `pad_token_id + 1 = 2`, so
the **usable** sequence length is **512**, not 514. The real headroom is therefore `512 − 483 = 29`
tokens, held by nothing but the measured drift being small. If a future model has a tighter budget,
or a tokenizer drifts harder at a cut edge, the chunk silently overflows and **the PERF-01 failure
comes straight back** — an `Expand` error, i.e. a silent NER downgrade by default and a **hard 400
on every large request** under `NER_REQUIRED`. The one thing chunking exists to prevent.

**Fix.** Make the bound a bound. After re-tokenizing a chunk in `infer_chunked`, check its actual
token count against the model's usable limit and act on it rather than hoping: either re-split that
chunk with a smaller window, or (simplest) treat the overflow as the error it is and let the
existing fail-closed channel handle it — but *knowingly*, with a message that names the cause,
instead of surfacing a raw ONNX `Expand` error. Either way, correct the doc comment: 480 bounds the
**window**, not the **sequence**; the sequence is `window + specials + edge drift`.

**Test.** A pure unit test alongside `chunk_tests` cannot see this (it has no tokenizer). It wants a
`--features onnx`, `#[ignore]`d case in `tests/ner_perf.rs`: tokenize an adversarial multi-script
field, run `chunk_char_ranges`, re-tokenize every window, and assert each is within the model's
usable limit — the assertion that would have made the 481–483 drift visible instead of incidental.

> **Closed 2026-07-14 — the bound is now *checked*, at the one place every path goes through.**
>
> **The constants say what they mean.** `MAX_SEQUENCE_TOKENS` (a name that claimed to bound the
> *sequence* while only bounding the *window*) is split in two: **`MAX_WINDOW_TOKENS` = 480** (the
> planning window) and **`MODEL_MAX_TOKENS` = 512** (the model's usable length — XLM-R's 514
> `max_position_embeddings` minus RoBERTa's position-id offset of 2). The sequence is
> `window + specials + edge drift`, and only the second constant bounds *that*.
>
> **The ceiling is enforced, not hoped for.** `run_and_decode` — the **single choke point** the direct
> call *and* every re-tokenized chunk pass through — now clamps any sequence over `MODEL_MAX_TOKENS`
> and emits a kind-free `warn!` (a count, never the text). This is a safety valve, and it degrades the
> failure from *"the ONNX graph errors, so the whole field gets no NER (or 400s under `NER_REQUIRED`)"*
> to *"the tail of one window isn't scanned"* — which the next window's 32-token overlap re-covers
> anyway. Structured PII is unaffected either way.
>
> **The relationships between the constants are compile-time invariants.** `const _: () = assert!(…)`
> pins `MAX_WINDOW_TOKENS < MODEL_MAX_TOKENS` (drift needs headroom) and
> `CHUNK_OVERLAP_TOKENS < MAX_WINDOW_TOKENS` (a window must advance, or chunking wouldn't terminate).
> Get either wrong and the crate **does not build** — strictly better than a runtime test, and it was
> `clippy::assertions_on_constants` that pointed it out.
>
> **The drift is now asserted, not incidental** —
> `tests/ner_perf.rs::m5_r2_every_retokenized_window_stays_within_the_models_usable_length`
> (`--features onnx`, `#[ignore]`d) drives the **real** `chunk_char_ranges` (made `pub` for exactly
> this: a copy of the logic in the test would drift from the code it guards) with the **real**
> tokenizer over six adversarial fields — Chinese, Japanese, Cyrillic, combining-mark/zalgo,
> mixed-script, and a 4 000-char run of `あ` with no spaces at all. It **independently reproduces the
> reviewer's measurement** — worst re-tokenized window **481–483** tokens in every case, against the
> 512 ceiling — and asserts each window both chunks (non-vacuity) and stays under the limit. The
> clamp is a valve that must never fire; this is what makes sure it doesn't.

---

<a id="m5-r3"></a>
### M5-R3 — the chunk slice hard-indexes tokenizer offsets — the one place on the masking path that can panic on attacker input

**Severity: hardening (low — fails closed; not reproduced).**

`src/pii/onnx.rs:160`:

```rust
let chunk = &input[char_start..char_end];
```

`char_start` / `char_end` come from `Encoding::get_offsets()` — tokenizer-derived **byte** offsets
(the doc comment calls them "char offsets", which is a second, smaller inaccuracy: they are byte
offsets that *happen* to land on char boundaries). Indexing a `&str` with a byte range that is not
on a `char` boundary **panics**.

This module already does not trust that. `ner_decode::decode_entities` (`ner_decode.rs:72,92-98`)
guards the *identical* class of offset with `text.get(start..end)` and a `warn!` — added by
**M2-R6**, whose whole subject was tokenizer offsets landing off a boundary. `Vault::mask`
(`anonymizer.rs:156-168`) does the same with an explicit `is_char_boundary` check, and
`overlap::materialize` (`overlap.rs:150-156`) spells the rule out:

> Never a panic: this is a proxy, and the input is attacker-influenced.

The new chunk slice is the only spot on that path that breaks the rule. Consequence if it ever
fires: the stage panics inside `spawn_blocking`, and per ARCHITECTURE the request is **blocked**
(400) — so it **fails closed, and is not a leak**. It is an availability hole on an unauthenticated
path, of the M4-R19 family.

**I could not reproduce it.** 13 adversarial inputs through the real tokenizer (Chinese, Japanese,
Cyrillic, emoji/flags, combining marks, zalgo, mixed-script glue, 20 000-char single token, base64
runs) produced **zero** off-boundary window edges. So this is a latent trap, not a live bug — but it
is a trap the codebase has already been bitten by once (M2-R6) and has *already decided* how to
handle everywhere else.

**Fix.** Use the same guard the rest of the module uses — `input.get(char_start..char_end)`, and on
`None` either widen to `char` boundaries (`overlap::widen_to_char_boundaries` is right there) or skip
the window with a kind-free `warn!`. Skipping costs recall on one window; panicking costs the
request.

**Test.** `chunk_tests` can pin it without a model: feed `chunk_char_ranges` an offsets table whose
window edge falls **inside** a multi-byte char (the `fake_offsets` helper builds tables directly), and
assert `infer_chunked`'s slicing step does not panic — the same shape as
`a_union_ending_inside_a_multibyte_char_still_covers_every_constituent` (`overlap.rs`), which exists
for exactly this reason on the resolver side.

> **Closed 2026-07-14 — and closed with the guard the codebase already had, rather than a second one.**
>
> `chunk_char_ranges` now takes `input: &str` and widens every window it emits through
> **`overlap::widen_to_char_boundaries`** — the *same* function the resolver uses, for the *same*
> reason, promoted from private to `pub(crate)` with a note saying so. Widening only ever *adds* bytes,
> so it cannot shrink a window's coverage, and it makes every returned range **sliceable by
> construction**. That was the right shape for the fix: the reviewer's point was not "add a check here"
> but "this module already decided how to handle tokenizer offsets everywhere else (M2-R6, M4-R14) and
> this one spot breaks the rule" — so the fix is to *apply the existing rule*, not invent a parallel one.
>
> `infer_chunked` still refuses to index: it uses `input.get(start..end)` with a `debug_assert!` plus a
> kind-free `warn!` + skip in release — the same drop-never-leak-never-panic shape as `Vault::mask`'s
> malformed-span guard. Belt and braces, deliberately: the range is now correct by construction, but
> this is a proxy on attacker-influenced input and a panic costs the request.
>
> **Test:** `chunk_tests::every_window_is_sliceable_even_when_an_offset_lands_inside_a_multibyte_char`
> feeds an offsets table that deliberately cuts a 3-byte `€` and a 4-byte `𝄞` in half, and asserts every
> returned range is `input.get(..)`-able. **It carries its own non-vacuity assertion** — it first checks
> the offsets table *really does* land off a `char` boundary, because a guard that silently stopped
> exercising its hazard is precisely how M4-R13 and M4-R24 stayed invisible. Ask what the corpus holds
> constant.

---

<a id="m5-r4"></a>
### M5-R4 — the fixpoint's convergence proof covers the recognizers, not the NER — and chunking newly routes large fields through it

**Severity: invariant / docs (low-medium — fails closed; the *next* planned model is the one likely to break it).**

`ARCHITECTURE.md:125-128` (and the same argument in `anonymizer.rs:68-72`):

> `Vault::mask_all` therefore re-detects until the text yields nothing. It converges because a
> placeholder is **inert** (no recognizer can match `[KIND_N]` or span across it), so each pass
> strictly shrinks the un-masked text.

The parenthesis is the proof, and it is a proof about **regex recognizers** — `[KIND_N]` has no `@`,
no `sk-`, not enough digits, and `[`/`]` are outside every character class. Sound, by construction.

But `mask_all` is called with the **`CompositeDetector`**, and the NER inside it is an ML model
under no such constraint. Nothing prevents XLM-R from tagging `[PERSON_1]` (or a dense run of
placeholders) as a `Person`/`Organization`. If it did, a pass would mask a placeholder, the text
would not strictly shrink, `MAX_MASK_PASSES` would be exhausted, and the request would **400** —
fail-closed (M4-R20 saw to that), so **not a leak**, but a hard availability failure on ordinary
input.

This is not new to M5 — but M5 is what makes it *reachable at scale*: before `12c9ea8`, a field over
~500 tokens didn't run the NER at all (it errored). Chunking now routes exactly the large,
placeholder-dense fields through it that would exercise this path.

**I checked, and it holds — for this model.** The real XLM-R int8 detector over 2 520 bytes of
placeholder-only text (large enough to be chunked) tags **0 entities**; full-hybrid `mask_all`
converges on 412 B / 6.6 KB / 26 KB fields. So the system is correct today. What is missing is the
*statement* that this is an **empirical property of the chosen model**, not a construction proof —
and therefore a **model-swap checkpoint**.

That matters more than it sounds, because the Backlog's designated successor is **GLiNER**, a
*zero-shot, open-label, context-driven* span extractor. A model whose whole design is "match by
context against labels you pass at inference" is *unusually* likely to look at
`Contact [PERSON_1] at [ORG_1]` and tag both. Whoever evaluates GLiNER must re-run this check, and
right now nothing tells them to.

**Fix.** Write it down next to the invariant it qualifies (`ARCHITECTURE.md`, *Masking must run to a
fixpoint*): placeholder inertness is proved by construction for the **structured recognizers** and
verified **empirically** for the current NER (XLM-R int8: 0 entities on placeholder-only text); a
**new NER model must re-verify it**, and if it fails, the failure is a 400 (fail-closed), not a leak.
Mirror the one-liner in `anonymizer.rs::mask_all`'s doc comment, whose convergence argument has the
same gap.

**Test.** A `--features onnx`, `#[ignore]`d case (`tests/ner_perf.rs` or `ner_eval.rs`):
`detector.detect("Contact [PERSON_1] at [ORG_1] in [LOCATION_1] about [EMAIL_1]. ".repeat(40))` must
return **no** entities — i.e. the model treats placeholders as inert. It is one assertion, it runs
only when a model is configured, and it is exactly the check a model swap must not skip.

> **Closed 2026-07-14.** The qualification is now written down **next to the invariant it qualifies**,
> in all three places the invariant is stated: `ARCHITECTURE.md` → *Masking must run to a fixpoint*
> (the full argument, plus why GLiNER makes it urgent), and `Vault::mask_all`'s doc comment
> (`anonymizer.rs`), whose convergence argument had the identical gap. The claim is now precise:
> placeholder inertness is proved **by construction** for the deterministic recognizers and holds
> **empirically** for the current NER — making it a **model-swap checkpoint**, not a theorem.
>
> **Test:** `tests/ner_perf.rs::m5_r4_the_ner_treats_placeholders_as_inert` (`--features onnx`,
> `#[ignore]`d) asserts XLM-R tags **zero** entities across a 3 040-byte placeholder-only field (large
> enough to exercise the *chunked* path — the one M5 newly opened), and that the **full hybrid**
> `mask_all` reaches its fixpoint on it rather than exhausting `MAX_MASK_PASSES` into a 400. It passes.
>
> The failure mode it guards is worth restating, because it is the *opposite* of the usual one here:
> if a future model tags placeholders, nothing leaks — masking is **fail-closed**, so the request 400s
> (M4-R20). The cost is **availability**, on ordinary input. That is why this is a checkpoint and not a
> footnote: **GLiNER**, the Backlog's designated successor, is a *zero-shot, open-label,
> **context**-driven* extractor — a model whose entire design is "match by context against labels you
> pass at inference", i.e. exactly the kind that could read `Contact [PERSON_1] at [ORG_1]` and tag
> both. Whoever evaluates it now has a one-line test telling them so.

---

<a id="m5-r5"></a>
### M5-R5 — CI never exercises the declared MSRV

**Severity: low.**

`Cargo.toml:5-7` declares `rust-version = "1.82"` — the M4-R22 fix, whose stated purpose is "keeps
CI from picking an older toolchain that cannot compile the crate". But both CI jobs pin
`dtolnay/rust-toolchain@stable` (`.github/workflows/ci.yml:17,40`), so **nothing ever builds the
crate on 1.82**. `rust-version` only makes `cargo` refuse a *too-old* toolchain; it cannot detect
that the code has drifted *past* the declared floor. The day someone uses a 1.85 API, CI stays green
and the MSRV claim becomes fiction — which is the same failure the field was added to prevent.

ROADMAP's M5 section states "CI has its MSRV ([M4-R22](reviews/M4.md#m4-r22))" — true of the
manifest, not of CI.

**Fix.** Either add a third matrix leg pinned to the MSRV (`dtolnay/rust-toolchain@1.82`, `cargo
check` is enough — it need not run the suite), or drop the ROADMAP claim to what is true: the MSRV is
*declared*, not *verified*. Declaring-without-verifying is a defensible tradeoff for a solo project;
saying CI enforces it is not.

**Test.** The CI job *is* the test.

> **Closed 2026-07-14 — and it was not the small one. The declared MSRV was fiction.**
>
> The finding said `rust-version` "cannot detect that the code has drifted *past* the declared floor".
> It had. Rather than pick between the two offered fixes, I **measured** — and `1.82` cannot so much as
> *parse* the dependency tree:
>
> ```text
> $ cargo +1.82 check --all-targets
> error: failed to parse manifest at `…/idna_adapter-1.2.2/Cargo.toml`
> Caused by:
>   feature `edition2024` is required
> ```
>
> The **real** floors, measured per feature set (`cargo +<ver> check --all-targets [--features onnx]`):
>
> | build | declared | 1.85 | 1.86 | 1.89 | **true floor** |
> |---|---|---|---|---|---|
> | default | 1.82 | ✗ `icu_*` / `idna_adapter` need 1.86 | ✓ | ✓ | **1.86** |
> | `--features onnx` | 1.82 | ✗ | ✗ `redb` ← `hf-xet` needs 1.89 | ✓ | **1.89** |
>
> **The MSRV differs per feature set — which a single `rust-version` cannot express.** `Cargo.toml` now
> declares **1.86**, the floor of the *default* (shipped, native-dep-free) build, and documents that
> `--features onnx` needs **1.89**; building onnx on 1.86–1.88 fails with cargo's own explicit
> `redb@3.1.3 requires rustc 1.89`, which is self-explanatory. Declaring 1.89 instead would have lied in
> the other direction — refusing a perfectly good default build on 1.86.
>
> **Fix:** a new `msrv` matrix job in `.github/workflows/ci.yml` **builds** both floors
> (`dtolnay/rust-toolchain@master` pinned to `1.86` / `1.89`, `cargo check --all-targets`). `check` is
> enough — `test` runs on stable in the job above; this one answers only *"does it still compile on the
> oldest toolchain we promise?"*.
>
> **The lesson, and it is the M4-R22 lesson finally landing.** M4-R22 added `rust-version` *to prevent
> this exact failure*, and it structurally could not: the field makes cargo refuse a too-**old**
> toolchain, but nothing makes the *crate* stay compatible with it. A declared MSRV with no job building
> on it is not a floor — it is a comment that looks like a guarantee. **Only a job that builds on the
> MSRV can hold the MSRV.** The finding was filed `low`; it was really the round's most load-bearing
> one, because it is the only one where the *documentation of a guarantee* had silently become false.

> **Addendum (2026-07-14, product decision) — collapsed to ONE floor: `1.89`.**
>
> The closure above declared **1.86** (the default build's floor) and *documented* 1.89 for `onnx`,
> reasoning that `rust-version` can only hold one value and 1.89 would over-restrict the default build.
> That reasoning is sound in the abstract and wrong for **this** product: it will always be deployed with
> **`onnx` on** — that is what the hybrid *is* (structured recognizers **and** NER). A "default-build
> MSRV" is therefore a promise about a configuration nobody runs: a **second number to keep honest,
> buying nothing** — and a second number is exactly the shape of drift this whole review round is about
> (*one home for a fact*).
>
> So: `rust-version = "1.89"`, the floor of the **real** product; the `msrv` CI job collapses from a
> two-leg matrix to a single `cargo check --all-targets --features onnx` on 1.89. The default build still
> *happens* to compile on 1.86 — we simply do not promise it, and nothing has to stay true about it.
>
> **It paid for itself immediately.** Raising `rust-version` un-gated an MSRV-aware clippy lint that
> `1.82` had been silently suppressing (`clippy::manual_is_multiple_of`, stable since 1.87) — on
> `luhn_valid`, the credit-card checksum. Semantically identical (`sum % 10 == 0` →
> `sum.is_multiple_of(10)`, both `u32`) and the corpus tests confirm no behaviour change, but it is a
> neat demonstration of the finding's own thesis: **an under-declared MSRV doesn't just fail to protect
> you — it actively hides the tooling that would.**

---

<a id="m5-r6"></a>
### M5-R6 — the READMEs' Status section is self-referentially stale and understates the 1.0.0 gate

**Severity: docs (low).**

`README.md:125-131` (and its mirror `README.it.md`, *Stato*):

> **M0–M4 complete**, M5 (integration & performance testing) in progress. … `v0.4.0`, pre-1.0: the
> first tagged release (`1.0.0`) follows once **M5's README/CI pass lands**.

That pass **is this commit** (`442144c` — the README rewrite and both workflows). So the README names,
as a future trigger, the very change that contains it: read literally, `1.0.0` is due now.

ROADMAP holds a stricter and more honest bar (`ROADMAP.md:320-322`): `1.0.0` is cut "when a real CI
run is green and, ideally, the manual verification procedure below has actually been run once" — and
neither has happened (no PR has run the new CI; no tag has been pushed; no live provider call has been
made). The README, the file a newcomer reads first, is the one place that doesn't say so.

**Fix.** Restate the Status gate in both READMEs to match ROADMAP: M5 is code-complete with **one box
open** (the manual live-provider verification), the CI/release workflows are **written but never yet
run**, and `1.0.0` follows a first green CI run — ideally after the manual dual-run check. Keeping the
EN/IT pair in step is the existing docs policy.

**Test.** None — prose. Worth noting the *class*, though: this is the third M5 item
([M5-R1](#m5-r1), [M5-R5](#m5-r5), this) where a claim is true in one file and stale in another. The
project's own answer to that is already written down — *one home for a fact* — and it applies to
design claims exactly as it applies to findings.

> **Closed 2026-07-14.** Both READMEs' *Status* / *Stato* sections now state the gate ROADMAP actually
> holds, and name the two things that have **not** happened rather than implying they have: the CI and
> release workflows are **written but have never run** (no PR, no tag has exercised them), and the
> **end-to-end check against a real provider has not been performed** (the procedure and the opt-in
> smoke test exist; both need a real key). `1.0.0` is cut once CI is green on a real run, ideally after
> that manual verification — and the READMEs now defer to ROADMAP as authoritative instead of carrying
> a second, drifting copy of the status.
>
> Both files also carried a **second** stale claim the finding didn't reach: *"Requires Rust (stable,
> MSRV 1.82)"* — false, as [M5-R5](#m5-r5) established. They now say **1.86+** (default) / **1.89+**
> (`--features onnx`), verified by CI. EN and IT stay in step, per the docs policy.

---

### Promoted to the design docs this round

One rule that is **already correct** in the code but was written down nowhere, and which the next
person to touch either chunking or `PiiKind::priority` needs:

> **Chunking's boundary fragments are cleaned up by the resolver, not by the `dedup()`.**
> `infer_chunked`'s `dedup()` removes only **exact** duplicates. A window that cuts an entity in
> half emits a *truncated* fragment (`Mil` where the neighbouring window sees `Milan`), which is a
> different span and survives it. What removes it is `overlap::resolve_overlaps`' NER phase: all
> three NER kinds share `priority() == 0`, so it tiebreaks on **span length, descending**, takes the
> whole entity first, and drops the fragment as overlapping. **This is load-bearing:** give
> `Person` a higher priority than `Location` and a truncated boundary fragment could outrank the
> whole entity it was cut from.

Added to `ARCHITECTURE.md` → *NER chunking (M5, PERF-01)*. The rest of this round's lessons are the
**fixes** for the findings above, and belong in ARCHITECTURE / TESTING when those findings close —
not here.

---

## Round 1 — closure (2026-07-14)

**All six findings closed.** 132 tests green (default) / 145 + 6 `#[ignore]`d (`--features onnx`),
`fmt` + `clippy` clean on both feature sets; the four live NER guards re-run green against the real
XLM-R int8.

Two of the six were *bigger than filed*, and both for the same reason — the finding said "this claim
is unverified", and verifying it found the claim was **false**:

- **[M5-R5](#m5-r5) (filed `low`)** — the declared MSRV `1.82` could not even parse the dependency
  tree. Measured floors: **1.86** default, **1.89** onnx, and *they differ per feature set*. Now built
  by CI. **A declared MSRV with no job building on it is not a floor; it is a comment shaped like a
  guarantee.**
- **[M5-R2](#m5-r2)** — the constant that claimed to bound the sequence bounded only the *window*; the
  re-tokenized chunk was **always** over it (481–483). Now enforced at the single choke point, with the
  drift asserted against six adversarial scripts.

**What promoted into the design docs** (beyond the reviewer's own note on boundary fragments):

- `ARCHITECTURE.md` → *Masking must run to a fixpoint*: placeholder inertness is proved **by
  construction** for the recognizers and only **empirically** for the NER — a **model-swap checkpoint**,
  aimed squarely at GLiNER ([M5-R4](#m5-r4)). Mirrored on `Vault::mask_all`.
- `TESTING.md` → the *not covered here* box now **points at** ARCHITECTURE rather than restating it —
  removing the duplicate that let [M5-R1](#m5-r1) go stale in the first place.
- `src/pii/onnx.rs` → the chunking constants' relationships are **compile-time** invariants
  (`const _: () = assert!(…)`), not runtime tests: get one wrong and the crate does not build.

**The through-line of the round.** Five of the six findings are the same shape: *a claim that was true
when written, was never re-checked, and had quietly stopped being true* — the NER's cost model (R1), the
token bound (R2), the fixpoint's convergence proof (R4), the MSRV (R5), the release gate (R6). None of
them leaked. All of them were **documentation of a guarantee that had drifted from the guarantee**. The
project already had the answer written down for review findings — *one home for a fact* — and this round
is what applied it to **design claims**, which is where the drift actually lives.

---

## Review 2 — closure verification (2026-07-14)

Reviewed `215a1f6` (M5-R2/R3/R4) · `0ca00aa` (M5-R5) · `f3618ba` (docs). This is the "Review N+1" pass
that confirms round 1's ledger is genuinely clean, the same shape as Review 11 did for M4-R24.

**Five of the six closures hold exactly as claimed.** The sixth — [M5-R2](#m5-r2) — fixed the *mechanism*
it was filed against (the bound is now genuinely checked, at a choke point I confirmed is the only one)
but chose a **valve behavior** that inverts the fail-closed posture it was protecting → [M5-R7](#m5-r7).
Two smaller items follow from the same fix: the design doc it should have updated ([M5-R8](#m5-r8)) and a
constant its new guard hand-copied ([M5-R9](#m5-r9)).

### What I verified independently

**Build & suite.** `cargo fmt --check` clean; `cargo clippy --all-targets -- -D warnings` and the same
`--features onnx` both exit 0. `cargo test` **132 passed** (+1 `#[ignore]`d); `cargo test --features onnx`
**145 passed** (+6 `#[ignore]`d). Zero warnings on both feature sets. The counts in ROADMAP are accurate.

**The four live NER guards, re-run against the real XLM-R int8** — all green, and the M5-R2 guard
**independently reproduces my Review 1 measurement**:

```text
  chinese: 3480 chars, 2 windows, worst re-tokenized window 481 tokens (bound 480, ceiling 512)
 japanese: 5100 chars, 3 windows, worst 483   cyrillic: 5880 chars, 3 windows, worst 482
combining: 2880 chars, 2 windows, worst 481      mixed: 5700 chars, 3 windows, worst 482
no-spaces: 12000 chars, 9 windows, worst 483
```

Every field chunks (2–9 windows — the non-vacuity assertion is real, not decorative), and the 481–483
drift against a 512 ceiling is exactly what I measured off-repo in round 1. Recall re-measured linear:
**1.19 s / 4.70 s / 19.1 s** at 64×/256×/1024×, entities `192/192`, `771/768`, `3084/3072`.

**The MSRV floors — re-measured myself, both directions.** The builder's numbers are right, and 1.89 is
the *exact* onnx floor, not an over-declaration (1.88 fails too):

| toolchain | default | `--features onnx` |
|---|---|---|
| 1.82 | ✗ `idna_adapter` needs `edition2024` (cannot even parse the tree) | ✗ |
| 1.85 | ✗ cargo refuses: `icu_collections@2.2.0 requires rustc 1.86` | ✗ |
| **1.86** | **✓ (exit 0)** | ✗ `ort` 1.88, `konst`/`redb` 1.89 |
| 1.88 | ✓ | ✗ `konst@0.4.3` / `redb@3.1.3` require 1.89 |
| **1.89** | ✓ | **✓ (exit 0)** |

One nuance worth recording, because it makes the claim *stronger* rather than weaker: on 1.85 the crate's
own code compiles fine (`cargo +1.85 check --all-targets --ignore-rust-version` → exit 0). What fails is
**cargo**, which honours the `rust-version` a *dependency* declares. That is still the floor a user hits
on a plain `cargo build`, so declaring **1.86** is correct — and `Cargo.lock` is committed, so the CI
`msrv` job resolves the same pinned tree I measured rather than drifting on a fresh resolve.

**The CI YAML is well-formed.** `dtolnay/rust-toolchain@master` + `with: toolchain: <version>` is that
action's documented form for a pinned version (the `@stable` / `@1.x` tag forms cannot take a matrix
variable) — the `msrv` job is written correctly. It is still, like the rest of the workflows, **never yet
run** — the disclosed gap, not re-flagged.

**Is `run_and_decode` really the single choke point?** **Yes** — verified, not taken on trust.
`session.run` appears exactly twice in the crate (`onnx.rs:288` and `:296`), both inside `run_and_decode`;
its only callers are `infer` (the direct path, guarded by `len() <= MAX_WINDOW_TOKENS`) and
`infer_chunked` (once per window). There is no other way into the session.

**Does the clamp corrupt `decode_entities`' spans, or desynchronize the tensors?** **No.** `ids`,
`attention_mask` and `token_type_ids` are each sliced `[..seq]`, and `offsets` — deliberately left at full
length — is consumed as `offsets.iter().enumerate().take(seq)`, so `tags[i]` still describes the token the
model was actually fed at position `i`. `num_labels = logits.len() / seq` divides the tensor the model
returned *for the clamped `seq`*. `decode_entities` gets the chunk's own text with offsets into that same
text. The clamp can only **truncate** the scan; it cannot misalign or corrupt a span. That part of the fix
is sound. What is *not* sound is what truncation means to the caller — [M5-R7](#m5-r7).

**Drove the real binary end-to-end** (`--features onnx`, `NER_REQUIRED=1`, `RUST_LOG=…=trace`, mock
upstream, a **4 789-char** field with the PII deliberately at the very **tail** — the region only the last
window covers, and the one a clamp would silently drop):

| | result |
|---|---|
| upstream saw `Mario Rossi` / `Acme Corporation` / `Milano` / `bob@test.com` / `4111111111111111` | **no** (all five) |
| upstream saw `[PERSON_1]` `[ORG_1]` `[LOCATION_1]` `[EMAIL_1]` `[CARD_1]` + the augmentation message | **yes** (all) |
| client got all five raw values restored; any `[KIND_N]` left unresolved | **yes** / **none** |
| raw PII anywhere in the `trace` logs | **none** |
| the clamp's `warn!` fired | **never** |

### What I tried to break and couldn't

- **Can the clamp actually fire on live input?** I could not make it. An off-repo probe carrying
  `chunk_char_ranges` + `widen_to_char_boundaries` **verbatim from HEAD** ran the real tokenizer over 11
  further shapes beyond the builder's six — zero-width joiners, soft hyphens, C0 control chars, a field
  that is 20 000 zero-width spaces with prose only at the end, base64 runs, emoji/ZWJ/flag sequences,
  dense placeholders, no-space mixed-script glue, a 60 KB whitespace flood, and literal `<s>`/`<unk>` text.
  **Worst re-tokenized window across all 17 shapes: 483.** Zero over 512, zero empty windows. The valve is
  genuinely latent — which is exactly why its *semantics* matter more than its frequency ([M5-R7](#m5-r7)).
- **Does the tokenizer ever emit a `(0, 0)` sentinel offset mid-sequence?** This is an **unstated
  assumption** `chunk_char_ranges` rests on: `char_end = offsets[token_end - 1].1.max(char_start)` would
  collapse to a **zero-length window** (a silently unscanned window) if a middle token carried the `(0, 0)`
  the specials use, and `char_start = offsets[token_start].0` would jump a window back to byte 0. Across
  all 17 shapes: **zero** mid-sequence `(0, 0)` offsets. It holds — but nothing *says* it holds, and the
  code would misbehave quietly rather than loudly if it stopped. It belongs in the rewritten ARCHITECTURE
  section ([M5-R8](#m5-r8)), next to the thing it governs.
- **Does widening ever open a coverage gap between consecutive windows?** **No, and by construction.**
  `widen_to_char_boundaries` (`overlap.rs:213-218`) moves `start` only **down** and `end` only **up**, so
  every window can only *grow*: consecutive windows can gain overlap, never lose it. The overlap itself is
  guaranteed independently — stride 448 < window 480, so window *i+1*'s first token index is strictly
  inside window *i*. M5-R3's fix is the right shape and costs nothing.
- **Can widening push a window over the ceiling?** At most 3 bytes per edge (widening stops at the first
  `char` boundary; UTF-8's longest encoding is 4 bytes) — a token or two at most, and the measurement above
  says it doesn't: 483 against 512, with the same 29 tokens of headroom as before.

### Verdict per round-1 finding

| | closure | verified |
|---|---|---|
| [M5-R1](#m5-r1) | TESTING points at ARCHITECTURE instead of restating | **holds** — but the file it now points at went stale ([M5-R8](#m5-r8)) |
| [M5-R2](#m5-r2) | bound checked at the choke point; compile-time invariants; live guard | **mechanism holds, valve semantics don't** → [M5-R7](#m5-r7), [M5-R9](#m5-r9) |
| [M5-R3](#m5-r3) | ranges sliceable by construction via the resolver's own widener | **holds** — no gap, no ceiling cost, guard non-vacuous |
| [M5-R4](#m5-r4) | inertness documented as empirical + a model-swap checkpoint | **holds** — guard passes, and it is the right guard |
| [M5-R5](#m5-r5) | MSRV measured (1.86 / 1.89) and built by CI | **holds** — re-measured both floors and both negative controls |
| [M5-R6](#m5-r6) | both READMEs restate the real 1.0.0 gate + MSRV | **holds** — EN/IT in step, both defer to ROADMAP |

---

<a id="m5-r7"></a>
### M5-R7 — the clamp trades `NER_REQUIRED`'s fail-closed block for a silent partial scan

**Severity: fail-closed / hardening (low-medium — latent; I could not make it fire).**

[M5-R2](#m5-r2) asked for the token bound to be *enforced* rather than hoped for, and offered two ways:
**re-split** the offending window, or **treat the overflow as the error it is** and let the existing
fail-closed channel handle it — "but *knowingly*". The fix took a third path: `run_and_decode`
(`src/pii/onnx.rs:243-257`) **clamps and continues**.

```rust
let seq = full_len.min(MODEL_MAX_TOKENS);
if seq < full_len {
    tracing::warn!(tokens = full_len, limit = MODEL_MAX_TOKENS, "NER sequence exceeds …; clamping");
}
// … no `Err` anywhere on this path — it returns Ok(entities) for the scanned prefix.
```

That returns **`Ok`**. And `Ok` is precisely what `NER_REQUIRED` exists to *not* get:

- `server.rs:102-106` — when `NER_REQUIRED` is set the NER is pushed into the composite **unwrapped**;
  otherwise it is wrapped in `FailOpen`.
- `composite.rs:39` — `CompositeDetector::try_detect` does `detector.try_detect(input)?`, so an unwrapped
  detector's `Err` propagates → `PrivacyStage` blocks → **400**. That is the whole contract of the flag.
- **Before** the fix, an over-budget sequence produced the ONNX `Expand` error → `Err` → 400. Nothing was
  forwarded. I reproduced that error from the pre-fix commit in Review 1.
- **After** the fix, the same condition produces `Ok(partial)` → the request is **forwarded**, with the
  tail of that window never scanned by the NER. Any `Person` / `Organization` / `Location` sitting in the
  clamped tail goes upstream **in clear** — for an operator who explicitly asked to be blocked instead.

So the fix **relocated** the failure rather than closing it: it swapped a hard 400 for a silent recall
hole, in the one configuration whose entire purpose is to prefer the 400. That is the M4 retrospective's
signature pattern, and it is the highest-priority class in this project — an over-permissive skip on
unexpected input. CLAUDE.md's non-negotiable is *"on any unexpected input, shape, or endpoint, block or
scrub — never forward raw PII"*; the clamp forwards.

**Two claims in the same doc comment (`onnx.rs:240-242`) do not hold, and they are what make the choice
look safe:**

1. > "the next window's `CHUNK_OVERLAP_TOKENS` of overlap re-covers it"

   Not for the **last** window — it has no successor, so a clamp there leaves the *field's tail*
   permanently unscanned. (Even mid-field the overlap only absorbs a clamp of ≤ 32 tokens, i.e. a drift
   ≤ 64; beyond that an un-recovered band opens in the middle.)
2. > "degrades the failure from … *(or 400s under `NER_REQUIRED`)* to *the tail of one window isn't scanned*"

   This reads the 400 as the bad outcome. Under `NER_REQUIRED` the 400 **is the product**.

**Be fair to the clamp, though — it is right for the *other* posture.** In the **default** (fail-open)
build an `Err` is swallowed by `FailOpen`, so erroring costs the **whole field's** NER, while clamping
costs only a window tail. The clamp is strictly *better* for privacy there. The bug is not the clamp; it
is that the clamp makes the **detector** decide fail-open vs fail-closed, when that decision belongs to
the **caller** — and the caller already has a mechanism for exactly this.

**Fix.** Let the overflow reach the channel the codebase already owns (`try_detect` + `FailOpen`,
M2-R1/R2) instead of a second, parallel degradation path that bypasses it:

- *Minimum:* return `Err(anyhow!("NER sequence {full_len} exceeds the model's usable length
  {MODEL_MAX_TOKENS}"))` from `run_and_decode` — kind-free, naming the cause instead of surfacing a raw
  ONNX `Expand` error. `FailOpen` swallows it in the default posture exactly as designed; `NER_REQUIRED`
  400s, as promised.
- *Better:* **re-split** the offending window at a smaller size (halve until it fits) and only error if
  even a minimal window overflows. That keeps recall **and** fail-closed, and it terminates.

This is the same lesson [M5-R3](#m5-r3)'s closure note gets right and this one misses: *apply the rule the
module already has; do not invent a parallel one.*

**Test.** Nothing can reach the clamp today — `MAX_WINDOW_TOKENS` / `MODEL_MAX_TOKENS` are `const`, so no
test can force the valve, which is exactly why the semantic swap went unnoticed. Make them injectable
(fields on `OnnxNerDetector`, defaulted to the consts), then add a `--features onnx`, `#[ignore]`d guard
in `tests/ner_perf.rs`: build a detector with a deliberately tiny window so re-tokenized chunks *must*
exceed the ceiling, and assert that a **required** composite returns `Err` (blocks) rather than `Ok` with
a short entity list. The assertion is one line; the point is that the valve's behavior becomes *testable*
rather than a comment.

**The rule to promote when this closes** (into `ARCHITECTURE.md`, next to the fail-closed model): *a
detector may degrade its own recall, but it may never decide **for the caller** that degraded output is
acceptable. Fail-open vs fail-closed is `FailOpen`'s decision, and the only way to reach it is the
`try_detect` error channel. A silent valve inside a detector routes around the switch the operator set.*

> **Closed 2026-07-14 — the finding is right, and it caught me doing the M4 retrospective's signature
> move in a fresh file.** The clamp *did* relocate the failure rather than close it, and the tell is
> exactly the one the reviewer names: my own doc comment argued the 400 was the bad outcome, when under
> `NER_REQUIRED` **the 400 is the product**. Both propping claims are false as charged — the last window
> has no successor, so its overlap re-covers nothing.
>
> **Fix: the *minimum*, deliberately — not the "better".** `run_and_decode` now returns `Err` on an
> over-long sequence, naming the cause (kind-free, never the text) instead of surfacing a raw ONNX
> `Expand` error. That routes the condition into the channel the codebase already owns: `FailOpen`
> swallows it in the default posture (structured-only for that field, exactly as designed), and
> `NER_REQUIRED` — which pushes the detector in *unwrapped* — **blocks the request (400)**, as promised.
> The detector no longer has an opinion about the posture, which was the entire bug.
>
> **I did not take the suggested "better" (re-split the window until it fits), and the reason is the
> project's own bar.** Re-splitting keeps recall *and* fail-closed, so it is genuinely better on paper.
> But it adds a retry loop, a termination argument, and a coverage-gap argument (a shrunk window's stride
> no longer matches its neighbour's) to guard a path that **cannot currently be reached** — 32 tokens of
> headroom against a measured +1…+3 drift, over 17 adversarial scripts. That is complexity bought for a
> hypothetical, in the one module where complexity has already cost us twice. *Textbook & lean.* If the
> guard ever shows the drift closing on the ceiling, re-splitting is the fix and the finding records why.
>
> **On the suggested test (make the consts injectable so the valve is forceable): not needed any more,
> and that is the point.** The reviewer's test existed to pin *what the valve does* — but there is no
> valve now. The overflow is an ordinary `try_detect` error, and "a required detector's error blocks the
> request / a `FailOpen`-wrapped one is swallowed" is **already** pinned, by FC-04 and CMP-02. The fix
> didn't just remove the wrong behaviour; it removed the *category* of behaviour that needed its own
> bespoke guard. The trigger stays unreachable, and `m5_r2_…within_the_models_usable_length` is what
> keeps it that way.
>
> **Promoted** to `ARCHITECTURE.md` → *Robustness & fail-closed*, verbatim in spirit: **a detector may
> degrade its own recall, but it may never decide *for the caller* that degraded output is acceptable.**

---

<a id="m5-r8"></a>
### M5-R8 — ARCHITECTURE's *NER chunking* section still names the constant M5-R2 deleted — in the file M5-R1 just made the single home

**Severity: docs (medium — it is now the *only* home, by design).**

`docs/ARCHITECTURE.md:372-374`:

> **NER chunking (M5, PERF-01).** `OnnxNerDetector` tokenizes a field once; if it fits the model's
> sequence budget (`MAX_SEQUENCE_TOKENS`, conservatively under XLM-R's `max_position_embeddings`), it
> runs exactly as before (M2). …

Three things wrong, compounding:

1. **`MAX_SEQUENCE_TOKENS` no longer exists.** `grep -r MAX_SEQUENCE_TOKENS src/` returns **nothing** —
   [M5-R2](#m5-r2) split it into `MAX_WINDOW_TOKENS` (480) and `MODEL_MAX_TOKENS` (512). The design doc
   names a constant that would not compile.
2. **"conservatively under XLM-R's `max_position_embeddings`" is the exact framing M5-R2 refuted.** The
   window is *not* under the ceiling once re-tokenized — it lands at 481–483 — and the whole point of the
   fix is that the bound is now **checked**, not assumed. The doc still states the hope the finding killed.
3. **The section never mentions the ceiling, the choke point, or the clamp at all.** The runtime behavior
   the fix *introduced* — `run_and_decode` enforcing `MODEL_MAX_TOKENS`, and what it does when it fires —
   is documented in no design doc. (`TESTING.md` mentions the valve in passing, but it now explicitly
   defers: *"The mechanism lives in ARCHITECTURE.md → NER chunking; this file only points at it."*)

That deferral is what makes this more than a typo. [M5-R1](#m5-r1)'s fix was to stop duplicating the
mechanism and **point at ARCHITECTURE as the one home** — correctly. But the very next fix in the same
commit series left that one home stale, so the pointer now aims the reader at a deleted constant and a
superseded design. **This is M5-R1's own failure mode, one round later, on the file M5-R1 promoted.**
*(DEVLOG's mentions are fine — it is a chronological log, and `DEVLOG.md:35` explicitly narrates the
rename.)*

**Fix.** Rewrite the first paragraph of ARCHITECTURE → *NER chunking (M5, PERF-01)* around what actually
ships: the two constants and what each bounds (`MAX_WINDOW_TOKENS` = the **planning window**;
`MODEL_MAX_TOKENS` = the **model's usable length**, 514 − RoBERTa's position-id offset of 2); the fact
that a window is **re-tokenized** and therefore lands at `window + specials + drift` (481–483, measured);
and that the ceiling is **enforced at `run_and_decode`**, the single choke point — with whatever the
[M5-R7](#m5-r7) fix decides that enforcement *does*. While there, state the offsets assumption the
chunker rests on and that I verified holds: **the `(0, 0)` sentinel offset appears only at the sequence
ends** — a mid-sequence `(0, 0)` would collapse a window to zero length (silently unscanned) or restart it
at byte 0.

**Test.** None — prose. The class, though, is now three-for-three in M5 ([M5-R1](#m5-r1),
[M5-R6](#m5-r6), this): *a fact restated in two places, updated in one.* The answer is the project's own —
**one home for a fact** — and it only works if the home is kept.

> **Closed 2026-07-14.** ARCHITECTURE → *NER chunking (M5, PERF-01)* rewritten around what actually
> ships. It now carries: a **table** of the two constants and what each bounds (`MAX_WINDOW_TOKENS` = the
> planning window, 480; `MODEL_MAX_TOKENS` = the model's usable length, 512 = 514 − RoBERTa's position-id
> offset of 2); the fact that a window is **re-tokenized** and therefore lands at
> `window + specials + drift` (**481–483, measured**); that the 32-token gap is the drift headroom and a
> **compile-time invariant**; that the ceiling is enforced at `run_and_decode`, the single choke point;
> and — per [M5-R7](#m5-r7) — that overflow is an **`Err`, not a clamp**, because the posture is the
> caller's to choose.
>
> The refuted framing is not merely deleted, it is **kept as a warning**: *"this section used to say the
> window was 'conservatively under `max_position_embeddings`' — a single budget, assumed safe. That is
> precisely the hope M5-R2 refuted: the window **was** under the limit and the **sequence was not**, on
> every single chunk. **A bound you do not check is not a bound.**"* Deleting the sentence would have lost
> the lesson; the failure mode is *assuming*, and the doc now says so where the assumption used to live.
>
> Also written down, as asked: **the `(0, 0)` sentinel assumption** the chunker rests on — that it appears
> only at the sequence *ends*, so a mid-sequence one would collapse a window to zero length or restart it
> at byte 0 — together with the reviewer's verification (17 adversarial inputs, zero mid-sequence
> sentinels) and the consequence: **a tokenizer swap must re-check it.** That makes it a checkpoint of the
> same class as [M5-R4](#m5-r4)'s placeholder inertness, and it was previously an unstated assumption in
> nobody's head but the reviewer's.

---

<a id="m5-r9"></a>
### M5-R9 — the M5-R2 guard hand-copies the one constant it was made `pub` to avoid hand-copying

**Severity: guard (low).**

`tests/ner_perf.rs:167-172`:

```rust
let ranges = chunk_char_ranges(
    &input,
    full.get_offsets(),
    MAX_WINDOW_TOKENS,
    32, // CHUNK_OVERLAP_TOKENS — private; kept in step with src/pii/onnx.rs
);
```

`chunk_char_ranges` was made `pub` for a stated reason — and it is the right reason:

> *"`pub` so the live guard in `tests/ner_perf.rs` (M5-R2) can drive the **real** chunker … A copy of this
> logic in the test would drift from the code it is supposed to guard."* (`onnx.rs:377-379`)

Two of its three inputs come from the real module (`MAX_WINDOW_TOKENS`, `MODEL_MAX_TOKENS`). The third is
hand-copied, with a comment promising to keep it in step by hand — which is the mechanism the sentence
above rejects. And `CHUNK_OVERLAP_TOKENS` is the *most* likely of the three to be tuned: it is the
boundary-recall dial (ARCHITECTURE's own note reasons about "480-token windows on a 448-token stride").

**Failure scenario.** Someone raises `CHUNK_OVERLAP_TOKENS` to 64 to reduce boundary misses. Production's
stride becomes 416; the guard keeps planning on 448. It still asserts "≤ 512 on 480-token windows" and
still passes — but on cut edges **production no longer produces**. The 481–483 drift measurement quietly
stops describing the shipped chunker, and nothing says so. That is a guard going half-blind, which is the
M4-R13 / M4-R24 lesson (*ask what the guard holds constant*) at small scale.

**Fix.** `pub const CHUNK_OVERLAP_TOKENS` and import it in the test alongside the other two. One line, and
it makes the guard's fidelity structural instead of a promise in a comment.

**Test.** The guard *is* the test; this is what makes it faithful to the code it guards.

> **Closed 2026-07-14.** `CHUNK_OVERLAP_TOKENS` is now `pub`, carrying the same doc note as
> `chunk_char_ranges` — *exported so the live guard drives the real chunker; a hand-copied value here
> would be a second home for this fact, free to drift from the one that matters* — and
> `tests/ner_perf.rs` imports it. All **three** of the guard's inputs now come from the module it guards.
>
> Filed `low`, and worth a second's respect anyway: the finding is that **I wrote the right justification
> and then violated it in the same breath.** `chunk_char_ranges` was made `pub` precisely so the guard
> wouldn't hold a copy that could drift — and then the call hand-copied `32` with a comment promising to
> keep it in step by hand, which is the exact mechanism that sentence rejects. The failure it buys is a
> guard going *half*-blind: raise the overlap to 64 and the guard still passes, still asserts ≤ 512 on
> 480-token windows, and quietly stops describing the shipped chunker's cut edges. **A guard that shares
> only *some* of its subject's constants is measuring a program that doesn't exist.**
