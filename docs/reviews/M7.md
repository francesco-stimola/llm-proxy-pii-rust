# M7 — NER latency · review record

The full record of every M7 review finding: repro, mechanism, `file:line`, the **fix**, and the
**test** that would have caught it. The ledger (id · title · severity · status) lives in
[`../ROADMAP.md`](../ROADMAP.md#m7); a finding is never copied between the two.

---

<a id="review-1"></a>
## Review 1 — first landing (2026-07-17)

Reviewed `89d2ca9` (`feat/m7-ner-latency`) against `main`.

**Verified independently.** `cargo test` **85 lib green** + every integration bin green, no warnings;
`cargo test-onnx --lib` **100 lib green** (the +3 `thread_tests`, as claimed); `cargo fmt --check`
clean; `cargo clippy --all-targets -- -D warnings` and `cargo clippy-onnx -- -D warnings` both clean.
All four `#[ignore]`d `m7_latency` harnesses run green against the cached model. **Every count in the
commit message checks out.**

**What I re-measured** (same box: 6 cores / 12 threads, debug). The harness reproduces cleanly and
its *structure* is sound — the per-field decomposition is the right model of the walk, and the shape
guards are load-bearing exactly as claimed:

```text
=== fixture: 112 fields, 22823 bytes (22.3 KB) ===
part         fields     bytes        ms    ms/KB  passes
system            1      6151       755      126       2
tool desc        10      9482       458       49      10
schema desc     100      7060       964      140     100
user msg          1       130        29      228       2

fields needing >1 pass: 2/112 (6281 of 22823 bytes)
=== TURN TOTAL: 2.2633878s (114 detector calls over 29075 bytes) ===

system  →  [(Organization, "An")]
```

**The S0 refutation is real and reproduces exactly.** `found=[1, 0]` on the `system` field: the hybrid
tags `(Organization, "An")` in prose that carries no PII by construction, so the largest field in the
turn pays a second full NER scan. The plan's "boilerplate has ~zero PII → one pass" is false, and
`m7_s0_what_the_ner_finds_in_boilerplate_that_has_no_pii` is the right shape of test to have caught it
(it reports rather than asserts, which is correct — the number is a precision question, not a latency
one). **This is the most valuable thing in the milestone.**

**What I drove / tried to break.**
- **Is `intra_threads` really inert w.r.t. detection?** The M4-retrospective's signature move would be
  a perf knob that silently costs recall, and *every* recall guard in the repo pins `intra=1`
  (`ner_eval.rs:133`, `ner_perf.rs:43`) while production now ships `6`. Built a throwaway probe outside
  the repo (path-dep on the lib, `--features onnx`): 21 corpus inputs + a >512-token chunked input +
  the M7 system-prompt paragraph, at `intra ∈ {1,2,3,4,6,8,12}`, `pool=1` → **byte-identical entity
  sets including span offsets, 171 entities at every thread count.** *I could not break it.* The knob
  is genuinely a performance knob on this model + the CPU EP. The gap is that **nothing pins it** —
  [M7-R3](#m7-r3).
- **The oversubscription invariant.** Worked `derive_intra_threads` through by hand across the test's
  whole `(cores, pool)` grid. The arithmetic is correct and the `0`-handling is safe at every entry
  point I could find — including the non-obvious one: `NER_POOL_SIZE=0` → `derive_intra_threads(0, 12)`
  → `12`, and `load` clamps the pool to `1`, so the effective shape is `1 × 12` = 12 threads on 12
  cores. **Correct, and not by accident.** But the assert's `cores.max(pool)` term hides a regime
  ([M7-R4](#m7-r4)) and the pool's `0` is not filtered the way the intra's is ([M7-R5](#m7-r5)).
- **Re-ran the thread sweep three times.** This is where the milestone's write-up outruns its data —
  [M7-R2](#m7-r2). Two of the three "settled" conclusions replicate; the SMT one inverts.
- **Never-log-raw-PII**: the new `tracing::info!(model, pool_size, intra_threads, …)` carries integers
  and a path — no values, no user data. Clean. The harness prints entity text, which is correct and
  correctly justified in its doc comment (synthetic fixture, `#[ignore]`d, and the rule governs the
  product, not a fixture).
- **No detection code changed.** The diff touches `onnx.rs` (a new pure function + one `with_intra_threads`
  argument), `server.rs` (one env read), tests and docs. `recognizers.rs` / `overlap.rs` / `privacy.rs`
  / `stream.rs` are byte-unchanged, so M4/M5/M6's guards hold as verified.

**Overall: no leak, no fail-open, no detection regression — and the central engineering judgement (stop
at the bar; don't build S3/S4) is right and I'd defend it.** The findings are about the **measurement
and its record**, which for a milestone whose entire deliverable *is* a measurement is where the risk
lives. Two are material ([M7-R1](#m7-r1), [M7-R2](#m7-r2)): the headline number and the executable bar
describe a configuration the server does not ship, and the sweep's conclusions are drawn from n=1 runs,
one of which does not survive replication.

**On the builder's own six questions**, answered where the finding doesn't already:

1. **Is the fixture representative?** Yes, and the reasoning about *not* using the captured body is
   **correct and should not be reconsidered** — a masked capture would measure an NER pass that finds
   nothing and lie optimistically. Synthesizing the shape and asserting it is the right call. The
   register of the paragraphs, the 1–4 KB tool descriptions and the 100-small-fields tier all match
   real traffic. The one thing I'd flag is not the shape but the *size* — see M7-R1's last paragraph.
2. **`default_intra_threads` correctness** — correct; the `0` paths are safe (see above). The unit tests
   are **not vacuous** (I checked the pool > cores regime specifically: the assert still pins `intra == 1`
   there, so it has teeth), but the assertion does not prove the invariant its name states — M7-R4.
3. **Detection unchanged?** Verified empirically at seven thread counts — identical. M7-R3 is about the
   absence of a guard, not a regression.
4. **Is the stop decision honest?** **Yes.** The bar was declared before the numbers, both shapes clear
   it, and S3/S4 genuinely carry the risk the write-up says they do. Recording the 20–40 KB caveat *is*
   enough for the fixture-size question. What is not enough is extrapolating that caveat from the
   un-shipped shape — M7-R1.
5. **The deferred over-mask** — deferring is right ([M7-R7](#m7-r7) says so and explains why); the
   *recording* is in the wrong file.
6. **Docs** — the READMEs (both mirrors) are the **most honest artifact in the milestone**: they list
   both shapes and name the trade. The ROADMAP and DEVLOG are the ones that drift. Numbers verified
   individually; one unit slip ([M7-R6](#m7-r6)).

---

<a id="m7-r1"></a>
### M7-R1 — the headline "2.17 s" and the executable bar measure `NER_POOL_SIZE=1`, which the server does not default to · measurement · **closed**

**Finding.** M7's deliverable is a number, and the number the milestone reports is not the number the
product ships with.

`tests/m7_latency.rs:436` defaults the pool to **1**:

```rust
let pool = std::env::var("NER_POOL_SIZE").ok().and_then(|s| s.parse().ok()).unwrap_or(1);
```

`src/server.rs:163` defaults it to **2**:

```rust
let pool_size = std::env::var("NER_POOL_SIZE").ok().and_then(|s| s.parse().ok()).unwrap_or(2);
```

So `build_hybrid()` → `pool=1` → `default_intra_threads(1)` = **12**, while the shipped server →
`pool=2` → `default_intra_threads(2)` = **6**. **PERF-M7-01 — the test that makes M7's bar
executable — never runs the configuration `server.rs` produces.**

The divergence is deliberate and documented in the harness (*"the shape M7 measures against: the
personal proxy… one session, the whole box"*), and as a *choice of what to optimize* it is defensible —
M7 exists for the concurrency ≈ 1 case. The defect is that the resulting number is then reported as the
**default's**:

- `ROADMAP.md:553` — *"**Met: 2.17 s** with the derived thread default"* …
- `ROADMAP.md:561` — *"latency 4.58 s → **2.85 s** (1.61×)"*, describing **the same default**.

Both sentences are in the same milestone section and cannot both be true. `2.17` is `pool=1, intra=12`;
`2.85` is `pool=2, intra=6`, which is what an operator who sets nothing actually gets. The status row
(`ROADMAP.md:38`) then carries `2.17 s` unqualified as the product figure, and `TESTING.md:512` records
PERF-M7-01 as *"masked … as production does … Measured **2.17 s**"* — true of the *walk*, false of the
*config*. **`README.md:163-167` / `README.it.md:164-168` get this exactly right** (both rows, both
labelled), so the ledger currently contradicts the README.

**Three consequences, in increasing order of how much they matter.**

1. **The guard guards the wrong thing.** `assert!(turn.as_secs_f64() < 3.0)` has ~28% headroom against
   2.17 s. The shipped default sits at 2.85 s — **5% headroom** — and *nothing asserts it*. A future
   change that costs the default 10% ships green.
2. **The 20–40 KB caveat is extrapolated from the un-shipped shape.** DEVLOG:204-205 says the top of the
   range gives *"~3.7 s (personal shape)"* — and labels it, honestly. But 2.85 s × (40/22.3) ≈ **5.1 s**
   is the sentence for the shipped default, and it appears nowhere. "Over the bar by 23%" and "over the
   bar by 70%" are different messages.
3. **The CC battery will run at the default.** `MANUAL_VERIFICATION.md` never mentions `NER_POOL_SIZE`,
   so the human closing M7's last box gets 2.85 s/turn, not the *"~2.2 s"* TESTING.md:674 promises them.
   (Harmless for the battery's runtime; it is the same conflation.)

**Repro.** Three runs of `m7_s1_how_much_of_the_box_can_one_request_use` on this box, `2×6` (the shipped
default) vs `1×12` (the measured shape): `2203/2194`, `2342/2519`, `2338/2379` ms — **a tie**. So on my
box the gap the docs report (2847 vs 2092, 36%) does not reproduce at all; the two shapes perform the
same. That does not weaken this finding, it re-points it: *which* number is right is box-specific, but
*which configuration the guard measures* is not.

**Fix.** Make the harness mirror the server, and let the sweep carry the personal shape:
- `build_hybrid()` → `.unwrap_or(2)`, matching `server.rs:163`, so the bar assert guards the **shipped**
  config. The `pool=1` rows already exist in PERF-M7-03's sweep, which is where that shape belongs.
- Wherever a turn latency is quoted, name its shape. `ROADMAP.md:38` and `:553` should carry the
  **default's** number (2.85 s on the reference box) — the README's two-row table is the model.
- Re-derive the 40 KB extrapolation from the default, or state both.

**Test.** PERF-M7-01's `build_hybrid()` must not be able to drift from `server.rs`'s default again. The
cheap version is the `.unwrap_or(2)` above plus a comment tying it to `server.rs`; the version that
cannot drift is to assert the bar for **both** shapes (parametrize the existing test over
`[(2, derived), (1, derived)]` and assert `< 3 s` on each), which also documents the trade in the one
place that fails when it stops being true.

> **Closed** (2026-07-17). Took the version that cannot drift, on both counts.
>
> **The default now has exactly one home.** `onnx::DEFAULT_POOL_SIZE` + `onnx::resolve_pool_and_intra`
> own the whole policy — parse, `0`-filter, derive — and **`server.rs` and the harness both call it**.
> The two `unwrap_or`s that disagreed are gone, so this is structurally impossible rather than fixed:
> there is no second place left to write a different default in. `thread_tests::the_harness_and_the_server_cannot_disagree_about_the_default` pins it.
>
> **The bar moved to its own test and got both shapes** — `m7_s2_the_bar_holds_for_every_shipped_shape`
> (PERF-M7-05) asserts `< 3 s` on the default *and* on `NER_POOL_SIZE=1`, each resolved through the
> server's own function. Measured, best of 3: **default 2,462 ms · personal 2,106 ms** — so the
> shipped default clears the bar by 18%, not by the 5% the review computed from my single sample.
> PERF-M7-01 keeps the per-field breakdown and now **reports without asserting**: one sample against
> a ~40% spread is a coin flip, not a guard (that half is [M7-R2](#m7-r2)).
>
> **Every quoted figure now names its shape.** ROADMAP's status row and S2 box carry both numbers;
> DEVLOG's S1/S2 sections and the 20–40 KB extrapolation are re-derived from the **default**
> (~4.4 s at 40 KB, with the personal shape's ~3.8 s beside it); TESTING's battery promise says
> "~2.5 s at the default"; the READMEs — which the review found already correct — keep their two-row
> table as the model everything else was aligned to.
>
> **On the reviewer's re-measurement** (`2×6` vs `1×12` a tie on their runs, vs 36% in my docs): with
> 3 reps I get 2,462 vs 2,106 — a 17% gap, still inside this harness's between-run band. So the
> honest statement is that the two shapes are **close on this box**, and the finding's real point
> stands untouched: *which configuration the guard measures* was never a box-specific question.

<a id="m7-r2"></a>
### M7-R2 — the sweep draws settled conclusions from n=1 measurements, and the SMT conclusion does not replicate · measurement · **closed**

**Finding.** `m7_s1_how_much_of_the_box_can_one_request_use` runs **one warm-up turn and one measured
turn per configuration** (`tests/m7_latency.rs:638-639`). Three of its rows became stated conclusions in
DEVLOG:184-189 and were escalated in ROADMAP:576-580 — including this one:

> **"SMT may hurt; 6 may beat 12." It does not** — 12 logical threads beat 6 (2.09 s vs 2.46 s).
> Hyperthreading helps this int8 model. ***Both* of the "measure, don't reason" items resolved against
> the intuition that was written down.**

**It does not replicate.** Three independent re-runs, `1×6` vs `1×12`:

| run | `1×6` | `1×12` | winner |
|---|---|---|---|
| builder's, on record | 2,462 | 2,092 | 12, by 18% |
| mine #1 | **1,946** | 2,194 | **6, by 11%** |
| mine #2 | 2,717 | **2,519** | 12, by 8% |
| mine #3 | **2,311** | 2,379 | **6, by 3%** |

The sign flips run to run. And the **same configuration** (`1×6`) spans 1,946–2,717 ms across my three
runs — a **39% spread**, more than double the 18% effect the conclusion is read off. The measurement
design cannot distinguish the claim from noise, so the honest status of the SMT question is
**unresolved**, not *resolved against the intuition*. The write-up's rhetorical high point — *both*
"measure, don't reason" items resolved against intuition — rests on the one row that doesn't hold.

This is load-bearing, not trivia: `default_intra_threads` divides `available_parallelism()`, i.e. the
**logical** count. That divisor is the right choice *if* SMT helps. If SMT is neutral here — which is
what four runs actually show — then the base of the derivation is an open question the milestone
believes it closed.

**What does replicate, and should be kept exactly as written** — I checked each:
- **The pool is inert at concurrency 1.** `2×1` ≈ `1×1` in all three runs (4205/4214, 5595/5312,
  5248/5280). The S1a correction is solid.
- **Scaling is sublinear.** 12 threads bought 1.92× / 2.22× / 2.21× (claimed: 2.19×). Solid.
- **`pool=1` costs throughput.** I measured 0.504 vs 0.642 turns/s = **−21%** (claimed −23%). Solid —
  and this is the one that refuted the builder's own prior "it is not a trade at all", so it earns its
  place in the write-up.

**Also does not replicate:** the `2×6` vs `1×12` latency gap (docs: 2,847 vs 2,092, 36%; mine: a tie in
all three runs — see [M7-R1](#m7-r1)). Which means `README.md:170-172`'s *"set `NER_POOL_SIZE=1`: it is
the lowest-latency **and** lowest-RAM shape"* rests, on the latency half, on a gap I cannot reproduce.
**The RAM half is sound and independent** (each session holds its own copy of the weights — that is
arithmetic, not a benchmark), so the recommendation survives; its stated *reason* should lead with RAM.

**Fix.**
- Demote the SMT row to **unresolved / box-specific** in DEVLOG:188 and ROADMAP:578, and drop the
  "*both* items resolved against intuition" framing — one did (`pool=1` is not free), one didn't.
- Keep the three claims that replicate; they are the sweep's real output.
- Add the reference box's run-to-run spread next to the tables, so a future reader knows an 18% delta on
  this harness is inside the noise.
- `README.md:170-172`: lead the `NER_POOL_SIZE=1` recommendation with RAM; qualify the latency half.

**Test.** PERF-M7-03 should repeat each configuration **≥3×** and print **min and median**, not a single
sample. Minimum is the more meaningful statistic for a latency benchmark on a noisy box (it is the
closest to the interference-free cost), and printing the spread is what makes the "is this real?"
question answerable from the harness's own output instead of from a second reviewer's re-runs. That is
the guard that would have caught this: with three reps, the SMT row would have printed as a tie and
never become a conclusion.

> **Closed** (2026-07-17). **This is the finding I'd want a future builder to read**, so the closure
> keeps its reasoning rather than just its diff.
>
> **The claim is withdrawn, not softened.** SMT is recorded as **unresolved** in DEVLOG's S1 and
> ROADMAP's M7 box, and the *"both 'measure, don't reason' items resolved against intuition"* framing
> is gone — one did (`pool=1` is not free: two independent measurements plus a mechanism), one did
> not. My own re-run reproduced the reviewer's result immediately and unambiguously: `1×6` **2,651 ms**
> beat `1×12` **2,966 ms**, i.e. the opposite of what I had published, on the same box, same fixture.
>
> **The harness now makes the noise impossible to miss** (`REPS = 3`): the sweep prints
> **min / median / spread** per shape, and its footer says outright that `spread` *understates* the
> problem — it is within-run, while the same config drifts ~40% between runs — and lists which rows a
> **mechanism** backs (sublinear scaling; pool-inertness, which comes from the code, not the timings)
> versus which are unreadable (SMT). The bar (PERF-M7-05) takes the **minimum**, per the review.
>
> **What I'd have missed without this finding, and it is the important part:** `default_intra_threads`
> divides `available_parallelism()` — the **logical** count — which is the right base *only if SMT
> helps. The derivation I shipped rests on the one row that doesn't hold.* It stays as-is (dividing
> the logical count is the conventional default and nothing measured argues against it), but it is now
> recorded as an **open question**, not a settled one. That distinction is the whole finding.
>
> **README:** the `NER_POOL_SIZE=1` recommendation now leads with **RAM** — arithmetic, one session
> instead of two — and qualifies the latency half, with a pointer to the harness and its spread.
>
> **The meta-lesson is promoted into ROADMAP → M7 and DEVLOG:** *M7 exists because M5 measured the
> wrong shape; M7 then measured the right shape with the wrong design.* Fixing the corpus does not
> save you from reading a conclusion off a single sample. Third time in this repo (M4-R13 → PERF-01 →
> here), and the first time the blind spot was the **measurement** rather than the **fixture**.

<a id="m7-r3"></a>
### M7-R3 — nothing pins `intra_threads`' inertness w.r.t. detection, and every recall guard pins `intra=1` · invariant · **closed**

**Finding.** M7's whole safety argument is *"`intra_threads` is purely a performance knob"*. That claim
is **true** — I verified it (below) — and it is **asserted by no test**, because the two harnesses that
could notice deliberately hold the knob at its pre-M7 value:

```rust
// tests/ner_eval.rs:131-133
// pool=1, intra=1: this harness scores *recall*, not latency — pinning both knobs keeps a
// score reproducible across boxes with different core counts.
let ner = OnnxNerDetector::load(&model, &tokenizer, id2label, 1, 1, needs_tt)

// tests/ner_perf.rs:40-43
// pool=1, intra=1: these guards measure *recall* and *pass counts*, not wall clock, so they
// pin the historical single-threaded shape rather than inheriting M7's derived default …
OnnxNerDetector::load(&model, &tokenizer, id2label, 1, 1, false)
```

The reasoning is **good** — a recall score that moves with the runner's core count is worthless — and
the consequence is that **the knob production just changed is the one every recall guard holds at its old
value**. The M5-R4 placeholder-inertness guard and the chunking guards all pass, and would pass
identically if `intra > 1` mangled every span, because they never run with `intra > 1`.

**Why this is not paranoia.** ONNX Runtime's intra-op parallelism repartitions GEMM and reduction work
across threads; floating-point addition is not associative, so a different partition can change a logit
in its last bits. The BIO decode is a **per-token `argmax`** (`ner_decode`), so a near-tie between two
labels can flip on nothing but thread count — and a flipped `B-PER` is lost recall, which in this repo
is the thing a miss makes into a leak. This is precisely [M5-R4](M5.md#m5-r4)'s shape: *the convergence
proof covers the recognizers, not the NER; inertness is **empirical** for the model, and a model swap must
re-check it.*

**Verified (reviewer's throwaway probe, outside the repo — a path-dep binary on the lib with `--features
onnx`).** 21 inputs lifted from `tests/corpus/ner_cases.json`, plus a >512-token input (to exercise the
chunked path, where a window-boundary decision is most likely to differ), plus the M7 system-prompt
paragraph that produces the `("An", Organization)` hit. Fingerprinted every detection as
`(kind, span.start, span.end)` at `pool=1`, `intra ∈ {1,2,3,4,6,8,12}`:

```text
intra= 1 (the shape every recall guard pins): 171 entities
intra= 2: 171 entities, 0 inputs differ from the intra=1 baseline  [IDENTICAL]
intra= 3: 171 entities, 0 inputs differ from the intra=1 baseline  [IDENTICAL]
intra= 4: 171 entities, 0 inputs differ from the intra=1 baseline  [IDENTICAL]
intra= 6: 171 entities, 0 inputs differ from the intra=1 baseline  [IDENTICAL]
intra= 8: 171 entities, 0 inputs differ from the intra=1 baseline  [IDENTICAL]
intra=12: 171 entities, 0 inputs differ from the intra=1 baseline  [IDENTICAL]
```

**Byte-identical, spans included, at every thread count.** So: **no regression, and the milestone's claim
stands.** This finding is about the *guard*, not the code.

**Why it still matters.** The property is empirical, not structural, and two scheduled Backlog items would
invalidate it silently:
- **A different execution provider** (*GPU optimization → DirectML/CUDA* — the natural next step on this
  box) is where cross-thread-count non-determinism is genuinely *likely*, not merely possible.
- **A model swap** (*Evaluate GLiNER*) re-opens it, exactly as M5-R4 says.

Today the only thing standing between either of those and a silent recall change is a reviewer happening
to run this probe again.

**Fix.** Promote the rule to where it governs, per CLAUDE.md's *promote what outlives the review*:
- **`ARCHITECTURE.md`**, in the new *NER threading* section: **`NER_INTRA_THREADS` changes performance
  only. That it does not change *detection* is empirical — verified on XLM-R int8 + the CPU EP, identical
  spans at intra 1…12 — not guaranteed by the runtime. A model change or an execution-provider change must
  re-verify it.** Put it next to M5-R4's neighbouring rule; they are the same lesson about the same layer.
- **`TESTING.md`**, next to THREAD-01: say plainly that the recall guards pin `intra=1` *on purpose*, and
  that the inertness of `intra > 1` is therefore covered by the guard below and nothing else.

**Test.** An `#[ignore]`d live guard in `ner_perf.rs` — the file that already owns the live-NER guards:
detect the `ner_cases.json` inputs (plus one chunked input) at `intra=1` and at
`intra=available_parallelism()`, assert the `(kind, start, end)` sets are **identical**. ~15 lines, needs
no new fixture, and it is the test that turns "a reviewer checked once in July 2026" into something a
DirectML EP swap trips over.

> **Closed** (2026-07-17). Built the guard as specified, in `ner_perf.rs` —
> `m7_r3_intra_threads_changes_speed_not_detection` (**NER-THREAD-01**). Fingerprints
> `(kind, span.start, span.end)` — spans included, since a shifted span is a different mask even at an
> identical count — at `intra ∈ {1, 2, 4, 6, available_cores()}`, and asserts every set equals the
> `intra=1` baseline that the corpus scores were measured against.
>
> **Inputs chosen for where a partition difference could actually surface**, per the finding's own
> reasoning: prose with PER/ORG/LOC, a **>512-token field** (the chunked path, where a
> window-boundary decision is likeliest to differ — asserted large enough, so it cannot silently stop
> exercising it), CJK, Italian prose with structured PII, and the `"Anthropic's"` shape that produces
> the `("An", Organization)` hit — **a near-tie by construction, which is exactly the argmax this
> finding says can flip on thread count**. Result: **134 entities, identical at every count.** It
> carries a non-vacuity floor (`total > 10`) so a guard that detects nothing cannot pass by agreeing
> with itself.
>
> **Promoted, because the rule outlives the milestone.** ARCHITECTURE → *NER threading* now states it
> next to M5-R4's neighbouring rule, with the mechanism (ORT repartitions reductions; float addition
> is not associative; the BIO decode is a per-token argmax; a flipped `B-PER` is lost recall, and here
> a miss is what makes a leak) and the trigger: **a model swap or an EP swap must re-run this.**
> TESTING says plainly, next to THREAD-01, that the recall guards pin `intra=1` *on purpose* and that
> NER-THREAD-01 is therefore the only thing covering `intra > 1`.
>
> The reviewer's verdict — *no regression, the claim stands, this is about the guard* — is confirmed
> by my own independent run. What changed is that it is now the CI-adjacent harness saying so, not a
> person who thought to check.

<a id="m7-r4"></a>
### M7-R4 — THREAD-01's oversubscription assert exempts the one regime where the product *does* exceed the box · test-quality · **closed**

**Finding.** `src/pii/onnx.rs:487-497`:

```rust
fn the_two_knobs_multiply_to_at_most_the_box() {
    // Anything that lets the product exceed the core count is the oversubscription this
    // derivation exists to prevent (12 cores, intra=12, pool=2 → 24 threads …).
    for cores in [1usize, 2, 4, 6, 8, 12, 16, 64] {
        for pool in [1usize, 2, 3, 4, 8] {
            let intra = derive_intra_threads(pool, cores);
            assert!(intra >= 1, …);
            assert!(pool * intra <= cores.max(pool), …);   // ← the escape hatch
```

Work the two regimes:
- **`pool ≤ cores`**: `intra = floor(cores/pool)`, so `pool * intra ≤ cores`, and `cores.max(pool)` **is**
  `cores`. The assert says what its name says, and holds. ✔
- **`pool > cores`**: `intra` clamps to 1, product = `pool`, and `cores.max(pool)` widens to **`pool`** —
  so the assert reads `pool <= pool` and passes **while `pool` threads run on `cores` cores**. `pool=8,
  cores=2` → 8 threads on a 2-core box: real oversubscription, asserted green, by a test named
  `the_two_knobs_multiply_to_at_most_the_box` and commented *"anything that lets the product exceed the
  core count is the oversubscription this derivation exists to prevent"*.

**The assert is not vacuous** — I checked this specifically, since it is the obvious suspicion. In the
`pool > cores` regime it still pins `intra == 1` (any `intra ≥ 2` would make `pool*intra > pool` and
fail), so the test does have teeth in all 40 pairs. The problem is narrower and more interesting: the
`.max(pool)` term encodes a **real and unavoidable exception** — the derivation *cannot* fix `pool >
cores`, because `intra` floors at 1 and nothing clamps `NER_POOL_SIZE` — and it encodes it **silently**,
in a way that contradicts the comment three lines above it and the name three lines above that.

This propagates: `ARCHITECTURE.md:509-511` states the invariant **unconditionally** — *"the invariant is
that the product fits the box"* — which is false for `NER_POOL_SIZE > cores`, an env var an operator can
set to anything. (Not a new risk: `NER_POOL_SIZE` has been unclamped since M2, and a silly pool hits the
~400 MB-per-session RAM wall long before the thread wall. But M7 is the milestone that *declares* this
invariant, so M7 owns stating its domain.)

**Fix.**
- `ARCHITECTURE.md`: state the bound and its domain — *the derivation bounds `pool × intra` by the core
  count **while `pool ≤ cores`**; beyond that `pool` alone oversubscribes and nothing clamps it, because
  `intra` cannot go below 1. `NER_POOL_SIZE > cores` is an operator error the proxy does not defend
  against.*
- The test: either rename it to what it proves (`derived_intra_never_oversubscribes_beyond_the_pool_itself`)
  or — better — split the regimes and make the exception explicit: assert `pool * intra <= cores` for
  `pool <= cores`, and assert `intra == 1` for `pool > cores`, with the comment saying why that is the
  best available. Same coverage; the test then *documents* the limit instead of hiding it inside a `max`.

**Test.** This *is* the test. The change above is the fix.

> **Closed** (2026-07-17). Took the "better" option: the regimes are **split**, so the test documents
> the exception instead of hiding it in a `max`. Renamed
> `the_two_knobs_multiply_to_at_most_the_box_while_the_pool_fits_it` — the name now carries the
> domain. `pool <= cores` asserts the real invariant, `pool * intra <= cores`; `pool > cores` asserts
> `intra == 1`, with the comment saying why that is the best available (the derivation is out of
> moves — `intra` floors at 1 and nothing clamps `NER_POOL_SIZE`). Same coverage, minus the escape
> hatch.
>
> **ARCHITECTURE states the bound with its domain**, and says why that matters rather than just
> restating it: an invariant asserted unconditionally but true only in one regime is **worse** than a
> bounded one — it is exactly what let a green test green-light 8 threads on a 2-core box. The
> `NER_POOL_SIZE > cores` case is named as an operator error the proxy does not defend against, with
> the reviewer's observation that it hits the ~400 MB-per-session RAM wall long before the thread wall.
> `derive_intra_threads`' own doc comment carries the same domain, so it is stated where it is
> implemented as well as where it is designed.
>
> The reviewer's point that the assert was **not vacuous** (it still pins `intra == 1` in that regime)
> is why this is a rename-and-split rather than a rewrite: the coverage was fine, the *claim* was not.

<a id="m7-r5"></a>
### M7-R5 — `NER_POOL_SIZE=0` isn't filtered like `NER_INTRA_THREADS=0`, and the startup log then names a pool the process doesn't have · observability · **closed**

**Finding.** M7 added the new knob with a careful `0`-guard and a comment explaining exactly why
(`src/server.rs:167-172`):

```rust
let intra_threads = std::env::var("NER_INTRA_THREADS")
    .ok().and_then(|s| s.parse::<usize>().ok())
    .filter(|n| *n > 0)                              // 0 = unset, never ORT's "pick for me"
    .unwrap_or_else(|| crate::pii::onnx::default_intra_threads(pool_size));
```

…and left the adjacent, older knob asymmetric (`src/server.rs:160-163`):

```rust
let pool_size = std::env::var("NER_POOL_SIZE")
    .ok().and_then(|s| s.parse().ok())
    .unwrap_or(2);                                   // ← no `.filter(|n| *n > 0)`
```

**The behaviour is safe** — I traced it, and it is safe *by two independent accidents*, which is worth
saying out loud. `NER_POOL_SIZE=0` → `pool_size = 0` → `default_intra_threads(0)` survives only because
`derive_intra_threads` does `pool_size.max(1)` (`onnx.rs:139`) → `12`; and `load` clamps the pool
`0 → 1` (`onnx.rs:180`) in a **shadowed local**. Effective shape: `1 × 12`. Correct.

**The log is not.** `server.rs:220`:

```rust
tracing::info!(model, pool_size, intra_threads, "ONNX NER detector loaded");
```

`pool_size` here is the **unclamped** outer binding — `load`'s clamp is a shadow inside `load`. So
`NER_POOL_SIZE=0` prints `pool_size=0, intra_threads=12` while the process runs one session with twelve
threads. That line exists *because* M7 made `intra_threads` derived — its own comment says *"an operator
debugging latency must be able to see what was picked without reproducing the arithmetic"* — and in this
case the operator reads a pool of `0`, an intra of `12`, and cannot reconcile them with
`intra = cores/pool` by any arithmetic at all.

**Fix.** `.filter(|n| *n > 0)` on the pool read too, so both knobs treat `0` as unset, `load`'s clamp
becomes unreachable-by-construction from the server, and the logged value is the effective one. One line,
and it makes the two adjacent env reads say the same thing about the same edge case.

**Test.** `server::tests` already owns this surface (`required_ner_is_fatal_when_absent`). The honest
version needs the env read lifted out of `load_onnx_ner` into a small pure helper (e.g.
`resolve_pool_and_intra(pool_var, intra_var, cores) -> (usize, usize)`) — which is worth it anyway, since
that helper is the thing THREAD-01 would then be able to test end-to-end rather than only its arithmetic
core: assert `("0", unset)` and `(unset, "0")` both resolve to the same `(pool, intra)` as their
unset/clamped equivalents. If that refactor isn't wanted, clamp at the read site so the logged value is at
least true.

> **Closed** (2026-07-17). Took the honest version — `onnx::resolve_pool_and_intra(pool_var,
> intra_var, cores)`, exactly as specified — and it **paid for itself twice**, because the same
> helper is what makes [M7-R1](#m7-r1)'s harness/server drift structurally impossible. Two findings,
> one seam; the review found them separately and they had the same root: *the same policy resolved in
> more than one place*.
>
> Both knobs now filter `0` **and** unparseable input as unset. `server.rs` calls the helper once and
> hands the results straight to `load`, so the logged `pool_size`/`intra_threads` are the
> **effective** values by construction — the line can no longer print `pool_size=0, intra_threads=12`
> for a process running one session with twelve threads. `load`'s clamp stays as the last-ditch guard
> but is now unreachable from the server.
>
> `thread_tests::both_knobs_treat_zero_and_garbage_as_unset` pins the finding's own test spec —
> `("0", unset)`, `(unset, "0")`, `("0", "0")` and garbage all resolve to the unset `(pool, intra)`.
> `an_explicit_value_wins_over_the_derivation` pins the other half, including that deliberate
> oversubscription stays **possible**: an operator who says `intra=12` with `pool=2` gets it. We
> refuse to *default* into oversubscription, not to permit it — that distinction is the knob's whole
> reason to exist.
>
> The reviewer's "safe by two independent accidents" is the sentence that made this worth doing
> properly rather than with a one-line `.filter`. Safe-by-accident survives until someone edits one of
> the accidents.

<a id="m7-r6"></a>
### M7-R6 — the fixture is 22.3 KiB; every doc says 22.8 KB, and the per-KB columns use the other unit · docs · **closed**

**Finding.** The harness prints its own size:

```text
=== fixture: 112 fields, 22823 bytes (22.3 KB) ===
```

22,823 B ÷ 1024 = **22.3 KiB**. Every doc says **22.8 KB** — 22,823 ÷ 1000, i.e. decimal kB:
`ROADMAP.md:550`, `DEVLOG.md:113`, `:158`, `:204`, `TESTING.md:509`, `README.md:160`, `README.it.md:161`.

The docs are **internally** inconsistent, not merely unconventional: the `ms/KB` columns are computed by
the harness as `ms / (bytes/1024.0)` (`m7_latency.rs:545-549`, `:645`), so they are per-**KiB**. The
README therefore prints "22.8 KB" and "~128 ms/KB" in the same table, and 2.85 s ÷ 22.8 = 125, not 128.
One of the two numbers is in the wrong unit and they can't both be right.

Nothing downstream changes — no conclusion turns on 2%. It is on the record because this milestone's own
standard is that a number is verified against what the test prints, and this one wasn't.

**Fix.** Say **22.3 KiB** (or just `22,823 B`, which needs no unit argument) in all seven places, matching
the harness's own output. If decimal kB is preferred for the README's general audience, then the `ms/KB`
columns must be recomputed to match — but the cheaper fix is to quote the harness.

**Test.** N/A (docs). The fixture's size guard (`(20_000..50_000).contains(&total)`) is in bytes and is
correct; only the prose derived from it slipped.

> **Closed** (2026-07-17). Quoted the harness, which is the cheaper fix and the one this milestone's
> own standard demands: **22.3 KiB** (or `22,823 B` where the exact number reads better) in all seven
> places — ROADMAP, DEVLOG ×3, TESTING, both READMEs — and the per-KiB columns are now labelled
> `ms/KiB`, matching what the code divides by. The harness's own banner says `KiB` too, so the string
> a future reader copies is already right.
>
> Small, and worth the line it takes: the milestone's stated standard is *"every number verified
> against what the test prints"*, and this one wasn't. A rule that bends for 2% isn't a rule.

<a id="m7-r7"></a>
### M7-R7 — the `(Organization, "An")` over-mask: deferring is right, but its only durable home is the archive · tradeoff · **closed**

**Finding.** The builder asked whether deferring this was right. **It was, and I'd have made the same
call** — for the reasons given (it fails *toward* masking, so it is not a leak; it is
[M4-R6](M4.md#m4-r6)'s accepted precision class; precision work needs its own recall argument; M7's scope
is latency). Nothing below disputes the deferral. This is about where it is written down.

Today the over-mask lives in **DEVLOG 2026-07-16** (history — correct, and inert) and in the **ROADMAP
M7 S0 box** (a milestone section that closes and stops being read). `PERF-M7-02` reports it at runtime,
which is right but only tells you if you run it. Per CLAUDE.md — *"if a finding taught the codebase a
**rule** — a tradeoff we now accept on purpose — write it into `ARCHITECTURE.md`, next to the thing it
governs. A lesson left only in the archive is one the next builder re-learns the hard way"* — this
belongs in ARCHITECTURE, and it isn't there.

**Why it is not just another M4-R6 instance** (which is the argument for filing it under M4-R6 and moving
on):
- **Different mechanism.** M4-R6 is a *deterministic recognizer* over-matching pure-numeric IDs (~18% of
  9-digit tokens). This is the **NER** emitting a 2-character fragment of a word it half-recognized. The
  fix paths are unrelated: M4-R6's is context (GLiNER); this one's is a span-quality floor or a
  subword-boundary rule.
- **Different blast radius, and it is large.** M4-R6 mangles a number in user data. This mangles
  **`"Anthropic's"` → `"[ORG_1]thropic's"` in the system prompt**, on **every Claude Code turn** — the
  instruction text the model must read correctly to behave. It corrupts *boilerplate*, not payload, and
  the proxy's own augmentation prompt then tells the model `[ORG_1]` is a real value to use verbatim.
  Nobody had seen it because nobody had run the NER over a real system prompt (which is the S0 lesson
  itself, one level down).
- **It has a latency consequence M7 already priced.** It is *why* the biggest field in the turn pays two
  passes (~940 ms of the original 4.24 s). So it is the concrete link between the precision backlog and
  the S4 fixpoint lead — the ROADMAP says S4 is "more relevant than the plan concluded" *because of this
  bug*, and that connection dies with the milestone section.

**Fix.** Two short additions, no code:
- **`ARCHITECTURE.md`**, next to the NER/over-mask discussion: record the accepted tradeoff — *the NER may
  emit sub-word fragments of organization names (measured: `"An"` from `"Anthropic's"`), which over-mask
  boilerplate in system prompts. Accepted, same class as M4-R6: it fails toward masking. It costs the
  field a second fixpoint pass, which is a latency cost, not a correctness one.*
- **`ROADMAP.md` → Backlog → *Evaluate GLiNER***, which already names itself *"the clean **precision** path
  for M4-R6's accepted over-mask"*: add this as its second exhibit, with the note that it is an
  NER-span-quality problem and so is the one of the two that a **model** change could actually fix.

**Test.** None needed — `PERF-M7-02` already reports it, at the right level (reports, does not assert;
the right number is a precision question). Its catalog entry in `TESTING.md:517-522` is accurate.

> **Closed** (2026-07-17). Both additions made, no code — the deferral stands.
>
> **ARCHITECTURE** now carries the accepted tradeoff next to the NER discussion, with the three
> things that make it worth its own entry rather than a pointer to M4-R6: the **mechanism** differs
> (a recognizer over-matching numerics vs the NER emitting a sub-word fragment), the **blast radius**
> differs (it corrupts the *system prompt* on every turn — `"Anthropic's"` → `"[ORG_1]thropic's"` —
> and the augmentation prompt then tells the model `[ORG_1]` is a real value to use verbatim), and it
> has a **priced latency consequence** (the second fixpoint pass, ~940 ms of a 4.2 s turn), which is
> the concrete link between the precision backlog and the S4 lead.
>
> **ROADMAP → Backlog → Evaluate GLiNER** now names **both** exhibits explicitly, with what separates
> them: M4-R6's path out is *context* (GLiNER's premise), this one's is *span quality* — so it is the
> one of the two a **model change alone** could fix. That was the reviewer's sharpest observation
> here, and it is the reason filing this under M4-R6 would have lost information rather than saved a
> line.
>
> I asked whether deferring was right and the answer was yes, so the closure changes nothing about
> the decision — only about whether the next builder finds out. The finding's own framing is the
> keeper: *"nobody had seen it because nobody had run the NER over a real system prompt"* — which is
> the S0 lesson, one level down.

---

<a id="review-2"></a>
## Review 2 — closure verification (2026-07-17)

Reviewed `0be815f` (`feat/m7-ner-latency`) against round 1's `8ea8e8e`.

**Verified independently.** `cargo test` **85 lib green** + every integration bin green, no warnings;
`cargo test-onnx --lib` **103 lib green** (100 + the 3 new `thread_tests`); `cargo fmt --check` clean;
`cargo clippy --all-targets -- -D warnings` and `cargo clippy-onnx -- -D warnings` both clean. All five
`#[ignore]`d `ner_perf` guards green against the cached model. `m7_latency`: four of five green — **the
new bar test fails on my box**, which is [M7-R9](#m7-r9), not a regression.

**Verdict per round-1 finding: 5 of 7 hold outright, 2 hold with a crack.**

| finding | verdict |
|---|---|
| [M7-R1](#m7-r1) | **holds, structurally.** `resolve_pool_and_intra` really is the single home — see below. |
| [M7-R2](#m7-r2) | **the withdrawal holds; the *guard* does not** → [M7-R9](#m7-r9). |
| [M7-R3](#m7-r3) | **guard exists and has teeth — but one of its inputs is inert** → [M7-R8](#m7-r8). |
| [M7-R4](#m7-r4) | **holds, and loses nothing** — proved equivalent below. |
| [M7-R5](#m7-r5) | **holds.** The logged values are the effective ones by construction. |
| [M7-R6](#m7-r6) | **holds for the seven places it named**; the fix reached one place too far → [M7-R10](#m7-r10). |
| [M7-R7](#m7-r7) | **holds.** Both promotions are accurate and well-placed. |

**M7-R1/M7-R5 — is `resolve_pool_and_intra` really the single home? Yes, and I tried to break it.**
Grepped every reference to both env vars across the tree: `NER_POOL_SIZE` and `NER_INTRA_THREADS` are
read at exactly **two** call sites (`server.rs:166-167`, `m7_latency.rs:438-439`), and both hand the raw
`Option<&str>` straight to the same function. There is no third parse, no surviving `unwrap_or`, and no
path that reaches `load` with a value the resolver didn't produce. `load`'s clamps (`onnx.rs:227`, `:230`)
are now genuinely unreachable from the server: the resolver's `.filter(|n| *n > 0)` plus
`derive_intra_threads`' `.max(1)` guarantee `pool >= 1` and `intra >= 1` on every branch, so the logged
`pool_size`/`intra_threads` are the effective values — the `pool_size=0, intra_threads=12` line the
finding was about is now impossible to print. `default_intra_threads` survives as a `pub` wrapper with
**no callers left**; harmless (it takes `pool` as an argument, so it cannot re-introduce a *default*),
but it is dead API and the next reader will wonder which of the two entry points is the real one.

**M7-R4 — did the regime split lose coverage? No. It is exactly equivalent, and I worked the grid.**
Same 40 pairs. Of them, **9** land in `pool > cores` (cores 1 × pool {2,3,4,8}; cores 2 × pool {3,4,8};
cores 4 × pool 8; cores 6 × pool 8) and **31** in `pool <= cores`. In the `pool <= cores` branch,
`cores.max(pool)` *is* `cores`, so old and new assert the identical predicate. In the `pool > cores`
branch, the old `pool * intra <= pool` combined with the pre-existing `intra >= 1` pins `intra == 1` —
which is precisely what the new branch asserts directly. **Same teeth, and the exception is now written
down instead of hidden in a `max`.** The split is strictly an improvement in what the test *says*.

**What I drove / tried to break.**
- **NER-THREAD-01's vacuity** — the builder asked me to try to construct a case where it passes
  vacuously. I did not have to construct one: **it already does, for the input that matters most**
  ([M7-R8](#m7-r8)). The other checks on it hold — the `(kind, start, end)` fingerprint is the right
  granularity, the `"Anthropic's"` near-tie is genuinely in the set (PERF-M7-02 confirms it still
  fires), and the non-vacuity floor has teeth for the *aggregate*.
- **Re-ran the whole `ner_perf` battery.** 134 entities at intra 1/2/4/6/12, **0 inputs differing** —
  the builder's figure reproduces exactly, and M7-R3's underlying claim (the knob is inert for
  detection) survives its second independent check.
- **Every re-quoted figure checks out** except the ones [M7-R9](#m7-r9) is about. The harness prints
  `=== fixture: 112 fields, 22823 bytes (22.3 KiB) ===` — **22.3 KiB verified from the harness's own
  banner**, as M7-R6 demanded. The 40 KB extrapolations are arithmetic and correct in the unit they
  claim: 2,462 × 40960/22823 = **4,419 ms ~ 4.4 s** (default) and 2,106 × 1.7947 = **3,780 ms ~ 3.8 s**
  (personal). README's derived rates check: 20/22.3 = 0.9 ms/KiB; 2,547/22.3 = 114; 2,106/22.3 = 94.4.
  The old "~1,400× faster" claim was correctly restated as "well over 100×" (2,500/20 = 125×) — an
  unlogged improvement, and the right call.
- **Never-log-raw-PII**: unchanged surface. The new `resolve_pool_and_intra` is pure arithmetic over
  two integers; the startup log still carries integers and a path. Clean.
- **No detection code changed.** The diff touches `onnx.rs` (one new pure function, one const, one
  wrapper), `server.rs` (the call site), tests and docs. `recognizers.rs` / `overlap.rs` / `privacy.rs`
  / `stream.rs` are byte-unchanged, so M4/M5/M6's guards hold as previously verified.

**Overall: the closures are real work, not paperwork — M7-R1/R5's shared-root diagnosis was correct and
the seam it produced is the right one.** The two material new findings are the same shape as round 1's,
one level down, and that is worth saying plainly: **M7-R1 taught the milestone to name a number's
*shape*, and M7-R2 taught it to repeat a measurement — and the number still has an unnamed variable that
dominates both.** My box, idle and on battery, masks the shipped default in **3.9 s**. The fix for a
noisy measurement was min-of-3, which is the statistic that cannot see the noise the builder had already
diagnosed in their own footer.

---

<a id="m7-r8"></a>
### M7-R8 — NER-THREAD-01's ">512-token chunked field" is 442 tokens and never chunks; the assert that claims to pin it counts bytes · guard · **closed**

**Finding.** [M7-R3](#m7-r3)'s reason for naming a chunked input was mechanical, and the closure
repeated it: the chunked path is *"where a window-boundary decision is likeliest to differ"*. The guard
built for it does not go there.

`tests/ner_perf.rs:238-241`:

```rust
let long_field = SENTENCE.repeat(40);
assert!(
    long_field.len() > 2_000,
    "must exercise the chunked path, or this guard misses the likeliest divergence"
);
```

`SENTENCE` (`ner_perf.rs:54`) is 59 bytes. `repeat(40)` = **2,360 bytes**, which clears the assert by
18% — and chunking does not key on bytes. `infer_chunked` runs only when
`encoding.get_ids().len() > MAX_WINDOW_TOKENS` (`onnx.rs:266`), i.e. **> 480 tokens**.

**Repro** (throwaway probe outside the repo, path-dep on the lib + the real tokenizer, driving the real
`chunk_char_ranges`):

```text
MAX_WINDOW_TOKENS = 480; chunking triggers when encoding.get_ids().len() > 480

repeat(30):  1770 bytes   332 tokens  -> single pass (1 window)
repeat(34):  2006 bytes   376 tokens  -> single pass (1 window)   [passes the `> 2_000` assert]
repeat(35):  2065 bytes   387 tokens  -> single pass (1 window)   [passes the `> 2_000` assert]
repeat(40):  2360 bytes   442 tokens  -> single pass (1 window)   [passes the `> 2_000` assert]  <- the test's input
repeat(45):  2655 bytes   497 tokens  -> CHUNKED (2 windows)      [passes the `> 2_000` assert]
```

**So the test's long input is 442 tokens — 38 short of the trigger — and runs as a single pass.** None
of NER-THREAD-01's other five inputs is remotely near 480 tokens either, so **the guard covers zero
chunked inputs**. Every claim built on it is false today:

- `tests/ner_perf.rs:241` — *"must exercise the chunked path"*: it does not.
- `docs/TESTING.md:570` — *"fingerprints … over prose, **a chunked >512-token field**, CJK, …"*: 442
  tokens, unchunked, and 512 is the wrong constant anyway (the trigger is `MAX_WINDOW_TOKENS` = 480;
  512 is `MODEL_MAX_TOKENS`, the ceiling the *re-tokenized* window must stay under).
- [M7-R3](#m7-r3)'s closure note — *"a **>512-token field** (the chunked path …) — **asserted large
  enough, so it cannot silently stop exercising it**"*: the assert cannot enforce that property, in
  either direction. It permits 2,001 bytes ~ **376 tokens** (nowhere near chunking), and it would keep
  passing if `SENTENCE` were reworded shorter.

**Why it matters, beyond the label being wrong.** The knob is inert for detection — I have now verified
that twice, and R8 does not dispute it. What R8 disputes is the **guard's domain**. M7-R3 exists for the
Backlog's DirectML/CUDA EP swap and a GLiNER swap, and the closure sells NER-THREAD-01 as the thing
those must trip over. A window-boundary decision under a different EP is exactly the divergence the
finding predicted, and it is the one case the guard cannot see. This is the M4 retrospective's lesson 3
verbatim — *an invariant is only as strong as the set it quantifies over* — and lesson 6: **a quantity a
test never varies is a quantity the test cannot see**, here the token count the code branches on.

It is also [M5-R10](M5.md#m5-r10) again, one file over: *the assert pins an ordering in the wrong unit,
not the property the code depends on.* That finding is about `window < ceiling` standing in for the
drift headroom; this one is about `bytes > 2000` standing in for `tokens > MAX_WINDOW_TOKENS`. **Third
time this repo has asserted a proxy for the thing it meant.**

**Fix.** Assert the property the code branches on, in the unit it branches on, and drive the real
chunker rather than a byte proxy — everything needed is already imported into this file
([M5-R9](M5.md#m5-r9)'s rule: no second home for the constant):

```rust
let long_field = SENTENCE.repeat(60);            // ~660 tokens — comfortably past the window
let enc = load_tokenizer().encode(long_field.as_str(), true).expect("encode");
assert!(
    enc.get_ids().len() > MAX_WINDOW_TOKENS,
    "the long input must exceed the {MAX_WINDOW_TOKENS}-token window or this guard never reaches \
     infer_chunked — the likeliest place for two thread counts to disagree (M7-R8). Got {} tokens.",
    enc.get_ids().len()
);
```

`load_tokenizer()` (`ner_perf.rs:47`), `MAX_WINDOW_TOKENS` and `chunk_char_ranges` are all already in
scope. Asserting `chunk_char_ranges(&long_field, enc.get_offsets(), MAX_WINDOW_TOKENS,
CHUNK_OVERLAP_TOKENS).len() > 1` instead is equally good and arguably better — it names the chunker.
Then correct `TESTING.md:570` to what the input is (*"a field past the 480-token window, so it goes
through `infer_chunked`"*), and drop "512" — it is the wrong constant for this branch.

**Test.** This *is* the test; the change above is the fix. It should be re-run against the model: at
`repeat(60)` the long input crosses into 2 windows, and if the fingerprints still match at every intra
count (I expect they will), M7-R3's claim finally covers the case it was written for.

> **Closed** (2026-07-17). Took the fix as specified: `SENTENCE.repeat(60)`, and the assert now runs the
> **real tokenizer** and compares against the **real constant** the code branches on —
> `enc.get_ids().len() > MAX_WINDOW_TOKENS` — rather than a byte proxy. Verified: the long input is now
> past the window, so the guard reaches `infer_chunked` for the first time. Still **identical at intra
> 1 / 2 / 4 / 6 / 12**, so the conclusion did not change; the *coverage* did, and the coverage was the
> finding.
>
> *(This note first credited the 134 → 194 entity jump to "the chunked path". It is not — see
> [M7-R15](#m7-r15). 194 − 134 = 60 = the 20 extra sentences `repeat(60)` adds, × 3 entities each.
> The chunked path contributes nothing to the **count**; the proof it is reached is the **token
> count** the assert now checks. Attributing a number to the mechanism I wanted it to demonstrate,
> inside the closure of a finding about exactly that, is its own small lesson.)*
>
> `TESTING.md`'s claim is corrected to "a field past the `MAX_WINDOW_TOKENS` chunking window (~660
> tokens)" — the previous ">512-token chunked field" was wrong twice, as the finding says: not chunked,
> and 512 is `MODEL_MAX_TOKENS`, the ceiling a *re-tokenized* window must stay under, not the trigger.
> The catalog entry now carries *why* the assert is in tokens, so the next person to touch it knows the
> byte version was tried and was hollow.
>
> **This is the one I'd have argued about and been wrong.** I wrote `assert!(long_field.len() > 2_000,
> "must exercise the chunked path")` and read it back as if the assert established the comment. It
> established nothing: 2,001 bytes is ~376 tokens, and the input I picked was 442 — confidently 38 short
> of a threshold I never checked. The reviewer's framing is the keeper and it is the third instance:
> **an assert in the wrong unit is not a weak guard, it is a guard for a different property** (M5-R10 on
> `window < ceiling`; M4's lesson 6 on the quantity a corpus never varies).

---

<a id="m7-r9"></a>
### M7-R9 — the bar's assert is decided by the box's power regime, which min-of-3 cannot see: it fails at 3.9 s on my box, and the milestone's headline never names the variable · measurement · **closed**

**Finding.** [M7-R2](#m7-r2) diagnosed *"the same configuration drifts ~40% between runs"* and the fix
took **min of 3 reps within one run**. Those are not the same statistic, and the builder's own harness
footer says so out loud (`m7_latency.rs:735-737`): *"`spread` is the within-run range; the same
configuration also **drifts BETWEEN runs**"*. **The guard was then built on the statistic that only sees
within-run.** Min-of-N removes jitter. It cannot remove a **regime shift**, because all N reps sit
inside the same regime — they agree tightly and confidently report the wrong number.

**Repro.** `m7_s2_the_bar_holds_for_every_shipped_shape`, my box (the same 6-core/12-thread box that
measured 2.2 s in round 1), idle, run **in isolation** (21 s total, nothing else running):

```text
=== S2: the ~3 s bar, 3 reps per shape, 22823 B turn (22.3 KiB) ===
default (NER_POOL_SIZE unset)    pool=2 intra=6   min    3943 ms   median    4093 ms

panicked at tests\m7_latency.rs:665:9:
default (NER_POOL_SIZE unset) (pool=2, intra=6) masks a realistic turn in 3943 ms (best of 3) —
over M7's ~3 s bar.
```

Twice, 3,843 ms and 3,943 ms — **31% over the bar**, against the builder's 2,462 ms. Note the *spread*
in those runs was **1–7%**, not 39%: within the regime the harness is tight. That is the whole point —
**min-of-3 was precise and wrong.**

**It is the box's regime, not a regression, and here is the independent evidence.** Two unchanged
benchmarks, untouched since M5, measured today against their recorded values:

| benchmark (code unchanged since M5) | recorded | mine, today | ratio |
|---|---|---|---|
| `onnx_ner_latency_and_recall_across_field_sizes`, 64× | 448 ms | 1,139 ms | **2.5×** |
| …256× | 2.07 s | 4.06 s | **2.0×** |
| …1024× | 7.53 s | 16.96 s | **2.3×** |
| S1 sweep `2×1` (pre-M7 shape) | ~4,700 ms | 9,038 ms | **1.9×** |

Everything on this box is ~2× slower than when it was recorded, on code that did not change. Cause,
found: **the box is on battery** (`Win32_Battery.BatteryStatus = 1`, 82%; plan "Balanced"; clock 2,489
of 3,201 MHz). Round 1's numbers were taken on AC. **Nothing in the harness records this, and nothing in
the docs names it.**

**Two consequences, and the second is the bigger one.**

1. **The guard is a box-state detector, not a regression detector.** A red `m7_s2` today means "the
   laptop is unplugged", and the message it prints — *"the milestone is not done, or a regression undid
   it"* — is actively misleading. Meanwhile, on AC the default has 2,462 ms against a 3,000 ms bar, so a
   genuine **20% regression** (2,462 → 2,954) still ships green. The guard fires on the thing that
   doesn't matter and is blind to the thing that does. That is [M7-R1](#m7-r1)'s complaint about the
   *previous* bar, arriving again through a different door.
2. **The milestone's central claim needs its regime named — and the regime is the *deployment's* normal
   state.** ROADMAP:38 and :555, DEVLOG's S2, TESTING's PERF-M7-05 and README:160-166 all state **2.46 s
   / 2.11 s** as *the* product figures. On this box, unplugged, the default is **3.9 s** and the personal
   shape is **~4.0 s** (S1's `1×12` row, min of 3) — **both miss the bar**. M7 exists for the personal
   proxy in front of Claude Code, i.e. **a laptop**, and a laptop's normal state is the one where the bar
   fails. The README's "measure on your own box" caveat is good and does not cover this: a user who
   measures on battery will conclude the docs are wrong by 60%, and they will be right.

   This is [M7-R1](#m7-r1)'s own lesson one level down. R1 said: *name the shape whenever you quote a
   number.* The fix named the shape — and the number has a **second** un-named variable that is larger
   than the one R1 caught (1.6× vs the shapes' 1.17×). *Naming one variable does not make a measurement
   reproducible; it makes the un-named ones harder to notice.*

**Fix — the assert and the claim, separately.**

- **Assert a ratio, report the absolute.** The regime cancels out of a ratio measured **in the same run,
  seconds apart**; it does not cancel out of a wall-clock number. M7's actual deliverable *is* a ratio —
  "the derived default is ~1.9× the pre-M7 `2×1` shape" — and it replicates across regimes where the
  absolute does not: builder on AC **1.85×** (4,700/2,547), me on battery **2.26×** (9,038/3,994). Have
  `m7_s2` measure `2×1` as its own in-run calibration leg (~9 s, on an `#[ignore]`d test) and assert
  `baseline / default > 1.5`, printing the absolute ms beside it. That guard catches a real regression
  in any regime and cannot fire because a laptop is unplugged.
- **Keep the ~3 s bar as a *reported product claim*, not an assert** — it is a claim about user-perceived
  latency on a reference box, which is exactly the kind of thing a hard assert on an uncontrolled box
  cannot hold. If an absolute assert is kept, it must be loose enough to only catch order-of-magnitude
  regressions (~8 s would still catch the 27 s → 2.5 s win) and its message must say "or your box is in
  a slow power state" first.
- **Record the regime next to every quoted number.** The reference-box footnote in ROADMAP/DEVLOG/README
  should read *"6 cores / 12 threads, **on AC**, debug build, best of 3"* — one clause, and it is the
  difference between a number a reader can reproduce and one they cannot. Ideally the harness prints it
  (a cheap in-run calibration leg does this without any platform API: report the turn as a multiple of
  `2×1`).
- **Report both shapes before asserting either.** `m7_s2` asserts inside the loop (`m7_latency.rs:654-670`),
  so the panic on the default meant **the personal shape never ran and never printed** — on a test whose
  stated purpose is a two-row comparison. Collect both rows, print both, then assert.

**Test.** The ratio assert above is the guard. The one that would have caught *this* is narrower and
worth having anyway: **PERF-M7-05 must print a calibration figure it did not compute from the same
sample it is asserting.** A bar with no calibration leg cannot distinguish "the code got slower" from
"the box got slower", and this milestone's whole thesis is that a measurement you cannot interrogate is
a measurement you should not conclude from.

> **Closed** (2026-07-17). **The finding confirmed itself while I was fixing it**, which is the part
> worth recording. The user plugged the box in and asked me to re-measure at full power; the shipped
> default came back at **4,933 ms** — *worse than the reviewer's battery number*, and 2× my own AC
> figure from hours earlier, with a within-run spread of **0.3%**. Three regimes, one code path:
> **2,462 / 3,943 / 4,933 ms**. The absolute is not a property of this code on this box, and no
> quantity of reps was ever going to reveal that.
>
> **The assert is now a ratio, exactly as prescribed.** `m7_s2` measures `PRE_M7_SHAPE` (`2×1`) as an
> in-run calibration leg and asserts `pre_m7 / shape > 1.5`. Measured across all three regimes:
> **1.85× (AC) · 2.26× (battery) · 2.10× (throttled AC)** — a 2× swing in the absolute, a stable ratio.
> The guard now catches a real regression in any regime and cannot go red because a laptop is
> unplugged. A **loose 8 s ceiling** stays for the order-of-magnitude case, and its message says *check
> your power state first*, per the finding.
>
> **Both rows are measured and printed before either is asserted** — the sub-point I'd have skipped and
> shouldn't have. Asserting inside the loop meant a failing default killed the run before the personal
> shape ever printed, on a test whose entire purpose is the two-row comparison. *A guard must not
> destroy the evidence you need to interpret it.*
>
> **The claim is re-stated, not just re-guarded** — this is where the finding cost me a headline and
> should have. Every quoted figure now names **AC** alongside the box. And the READMEs' two-row latency
> table is **gone**: this run had the *personal* shape at 5,554 ms against the default's 4,841 — the
> opposite ordering to the one they advertised. Measured honestly, the two shapes are a **wash on
> latency** (each has won by ~15% on different runs), so the recommendation now rests entirely on the
> half that is arithmetic: `NER_POOL_SIZE=1` **halves the RAM**. The reviewer said in round 1 that the
> READMEs were the milestone's most honest artifact; keeping that true meant deleting a row from them.
>
> **And the meta-point, which is M7-R1's lesson arriving one level down.** R1 taught this milestone to
> *name a number's shape*. The number had a second un-named variable, worth ~2× where the shapes were
> worth 1.17×. **Naming one variable does not make a measurement reproducible — it makes the un-named
> ones harder to notice**, because the number now looks qualified. M7's whole thesis is that a
> measurement you cannot interrogate is one you should not conclude from; the bar is now interrogable
> from its own output.
>
> **What it costs the stop decision, stated rather than buried:** the bar is met *for a realistic turn
> on a box at full speed*. Throttled it is ~4.9 s, and M7 exists for a proxy in front of Claude Code —
> a **laptop**, whose normal state is the throttled one. The stop stands (the ~2× is banked and
> regime-invariant; the rest is a power governor, not the code), but **S3 is now the named lead for
> both open cases**, and it is the right shape for them: the boilerplate is byte-identical every turn,
> so a content-keyed cache makes turn 2+ nearly free *regardless of regime*. Recorded in ROADMAP → M7
> and DEVLOG's S2.

---

<a id="m7-r10"></a>
### M7-R10 — M7-R6's unit fix reached one number too far: `903 ms/K(i)B` now appears in both units in the same file · docs · **closed**

**Finding.** [M7-R6](#m7-r6)'s rule was *"quote the harness"* — the fix is correct for the seven places
it named, all of which describe the M7 fixture, and I verified all seven against the harness's own
banner. But the closure also relabelled a figure that **does not come from that harness**, and did not
convert its siblings.

`docs/DEVLOG.md:119` (changed by this commit — was `903 ms/KB`):

> **A realistic turn masks in ~4.2–4.7 s — not 27 s.** The 903 ms/**KiB** headline came from a blob
> densely packed with Italian names…

The same number, in the same file, unconverted:

- `docs/DEVLOG.md:254` — *"The entry below reports **903 ms/KB**"*
- `docs/DEVLOG.md:455` — *"26,567 ms = **903 ms/KB**"*

The 903 figure is **not** an M7-harness output. It is hand-computed in the 2026-07-16 entry from a
pre-M7 live-trace measurement (`26,567 ms` over a `29.4 KB` field), and nothing records whether that
`29.4 KB` was `bytes/1024` or `bytes/1000` — so **which unit is right is unknown**, and the relabel
picked one. `ROADMAP.md:534` and `:627` carry the related `286 ms/KB` in the old unit too.

Nothing turns on it — this is 2% on a number whose only job is to be the "before". It is on the record
because it is precisely the class M7-R6 opened: *this milestone's standard is that a number is verified
against what the test prints*, and this one was relabelled **more confidently than it was known**, by the
very fix that established the standard. A rule applied past its evidence is a different failure from a
rule not applied, and the second one is easier to spot.

**Fix.** Pick one and make the file self-consistent. Cheapest and most honest: revert `DEVLOG:119` to
`903 ms/KB`, matching `:254` and `:455`, and leave the pre-M7 dataset in the unit it was computed in —
M7-R6's finding was scoped to the M7 fixture, and it should stay there. If the KiB reading is preferred,
all three (plus `ROADMAP:534`, `:627`) must move together, and the entry should say the conversion is
inferred, not measured. Same for the fixture's own first-draft size, recorded as `13.5 KB`
(`DEVLOG.md:114`) and `13.5 KiB` (`TESTING.md:511`).

**Test.** N/A (docs).

> **Closed** (2026-07-17). Took the cheapest and most honest option, as recommended: `DEVLOG:119`
> reverts to **903 ms/KB**, matching `:254` and `:455`, and the pre-M7 dataset stays in the unit it was
> computed in. The entry now says *why* in one clause — the figure is hand-computed from a live trace
> whose `29.4 KB` nobody recorded as ÷1024 or ÷1000, so **relabelling it would assert a unit nobody
> measured**. M7-R6's rule was scoped to the M7 fixture, whose harness prints its own divisor, and it
> stays there. (`13.5 KB` → `13.5 KiB` at `DEVLOG:116` *is* an M7-fixture figure and did move; TESTING
> already had it right.)
>
> The finding's own sentence is the lesson, and it is sharper than the fix: **a rule applied past its
> evidence is a different failure from a rule not applied, and the second one is easier to spot.** I
> established "quote the harness" and then, in the same commit, relabelled a number the harness never
> printed.

---

<a id="review-3"></a>
## Review 3 — closure verification (2026-07-17)

Reviewed `a334fc2` (`feat/m7-ner-latency`) against round 2's `0ab02ae`.

**Verified independently.** `cargo test` **85 lib green** + every integration bin green, no warnings;
`cargo test-onnx --lib` **103 lib green**; `cargo fmt --check` clean; `cargo clippy --all-targets --
-D warnings` and `cargo clippy-onnx -- -D warnings` both clean. All five `#[ignore]`d `ner_perf`
guards green; all five `m7_latency` harnesses green — **including the bar, which now passes on my
box**, where round 2's version failed. **Same box as rounds 1–2** (Ryzen 5 PRO 8540U, 6 cores / 12
threads), and this time **verified on AC**: `Win32_Battery.BatteryStatus = 2`, 99% charge.

**Verdict per round-2 finding: all 3 hold. The fixes are real.**

| finding | verdict |
|---|---|
| [M7-R8](#m7-r8) | **holds.** `repeat(60)` is genuinely past the window — measured, below. The guard reaches `infer_chunked` for the first time. One mis-attributed number → [M7-R15](#m7-r15). |
| [M7-R9](#m7-r9) | **the ratio design holds, and I stress-tested it harder than any run before it.** But its two constants and its central claim do not → [M7-R12](#m7-r12), [M7-R13](#m7-r13), [M7-R14](#m7-r14). |
| [M7-R10](#m7-r10) | **holds.** `DEVLOG:119` reads `903 ms/KB`, consistent with `:254` and `:455`; `13.5 KiB` at `:116` is an M7-fixture figure and correctly moved. Verified all five sites. |

**M7-R8 — is `repeat(60)` really past the window? Yes, and here is the curve** (throwaway probe
outside the repo, path-dep on the lib, driving the **real** tokenizer and the **real**
`chunk_char_ranges`):

```text
MAX_WINDOW_TOKENS = 480
repeat( 40):   2360 bytes    442 tokens  -> 1 window(s) single pass   <- the old input
repeat( 45):   2655 bytes    497 tokens  -> 2 window(s) CHUNKED
repeat( 60):   3540 bytes    662 tokens  -> 2 window(s) CHUNKED       <- the new input
```

**662 tokens, 2 windows — the fix does what it says**, and the assert now runs the tokenizer against
`MAX_WINDOW_TOKENS`, so it cannot silently stop being true. NER-THREAD-01 reproduced at **194
entities, 0 inputs differing at intra 1 / 2 / 4 / 6 / 12** — `intra_threads`' detection-inertness now
survives its **third** independent check, and for the first time that check covers the chunked path.
That was the finding, and it is closed.

**What I drove / tried to break.**
- **The ratio guard, under a regime nobody had tried.** Ran `m7_s2` two ways on the same box within
  the same hour, at constant power state: the **documented invocation** (all five perf tests, which
  cargo runs *concurrently*), and **isolated** (`--test-threads=1`). The absolute moved **1.50×**;
  **the ratio held at 1.81× and 2.12×.** That is the strongest evidence yet that M7-R9's central
  design decision is correct — and it is also how I found [M7-R12](#m7-r12), because the variable
  that moved the absolute was not the one the docs name.
- **Can the ratio go vacuous?** Yes, on a small box, and by construction —
  [M7-R13](#m7-r13). Worked `resolve_pool_and_intra` across the core-count grid: at `cores ≤ 2` the
  shipped default resolves to `(2, 1)`, which **is** `PRE_M7_SHAPE`.
- **Every re-quoted figure.** 194 entities ✔ (and I decomposed it — [M7-R15](#m7-r15)). 22.3 KiB ✔
  from the harness banner. The 1.85/2.26/2.10 ratios are consistent with my 1.81/2.12. The
  4,933/4,841/5,554 ms figures are consistent with my 4,757/5,416 — **and that consistency is the
  problem**, not the reassurance: see [M7-R12](#m7-r12).
- **Never-log-raw-PII**: unchanged surface. The diff touches tests and docs plus nothing in the
  logging path. Clean.
- **No detection code changed since round 2.** `recognizers.rs` / `overlap.rs` / `privacy.rs` /
  `stream.rs` / `onnx.rs` are byte-unchanged from `0be815f`, so M4/M5/M6's guards hold as verified.

**Overall: no leak, no fail-open, no detection regression — and M7-R9's ratio is the right
instrument, now confirmed against a fourth regime.** But this round's findings are the same shape as
the last two, one level down again, and the pattern has now run four deep: **R1 named the number's
*shape*; R9 named its *power regime*; R12 shows the power regime does not order the milestone's own
data** — battery (3,943 ms) is **faster** than three of the four AC measurements (4,757 / 4,841 /
4,933). A variable that cannot sort your observations is not the variable.

**On the builder's six questions**, answered where the finding doesn't already:

1. **Is the ratio guard sound?** **The ratio, yes** — I moved the absolute 1.50× at constant power and
   it held. **Its two constants, no** ([M7-R14](#m7-r14)), and **it goes vacuous below 4 cores**
   ([M7-R13](#m7-r13)). Is 1.5 the right floor? **Yes, given the data** — the worst ratio anyone has
   observed is my 1.81×, and a tighter floor would false-fire. But 1.5 against 1.81 tolerates a **17%
   regression**, which is the *same* blindness the docs say the ratio cured the 3 s bar of.
2. **M7-R8's fix** — genuinely past the window (662 tokens), genuinely reaches `infer_chunked`. **194
   vs 134 is not explained by chunking** — [M7-R15](#m7-r15).
3. **The deleted README table** — **no, you did not over-correct; the data support "a wash" more than
   you knew.** My two runs *invert*: contended, personal wins by 11%; isolated, default wins by 14%.
   That is a fourth and fifth inversion. Deleting the table was right. **But the correction did not
   reach ROADMAP:555 or DEVLOG:226**, which still advertise the ordering — [M7-R11](#m7-r11).
4. **The stop decision** — **still defensible, but not on the grounds the ROADMAP states.** See
   [M7-R12](#m7-r12): "the bar is met" rests on one observation out of six.
5. **Every number** — checked individually; two are wrong ([M7-R15](#m7-r15)) or unsupported
   ([M7-R11](#m7-r11), [M7-R12](#m7-r12)).
6. **Ledger/record consistency** — all 10 findings have exactly one home, anchors resolve, no
   double-recording, and the ARCHITECTURE/TESTING promotions are **accurate** (I re-read the *NER
   threading*, over-mask and THREAD-01 sections against the code). The one drift is
   [M7-R11](#m7-r11).

---

<a id="m7-r11"></a>
### M7-R11 — the READMEs dropped the two-shape latency table as unsupportable; ROADMAP and DEVLOG still advertise it · docs · **closed**

**Finding.** [M7-R9](#m7-r9)'s closure deleted the READMEs' two-row table and recorded why, in its own
words:

> Measured honestly, the two shapes are a **wash on latency** (each has won by ~15% on different
> runs), so the recommendation now rests entirely on the half that is arithmetic.

`README.md:167` now says *"hybrid (either shape — see below) — ~2.5 s"* and `:182` says *"**Latency
between the two shapes is a wash**"*. **Correct, and well supported.** But the same commit left the
superseded comparison standing in the two places the project treats as authoritative:

- `docs/ROADMAP.md:555` — *"**Met on both shipped shapes** — **2.46 s** at the default … and **2.11
  s** at the single-client shape"*
- `docs/DEVLOG.md:226` — *"**2.46 s** at the default (pool 2 × intra 6) and **2.11 s** at
  `NER_POOL_SIZE=1` (1 × 12), best of 3, **on AC**."*

Both present the personal shape as **14% faster** — the exact ordering the closure declared it could
not support, from the exact run it superseded.

**Repro — the ordering inverts inside a single session.** Same box, same AC state, ~40 minutes apart:

| run | default (2×6) | personal (1×12) | winner |
|---|---|---|---|
| contended (the documented invocation) | 7,142 ms | **6,448 ms** | **personal, by 11%** |
| isolated (`--test-threads=1`) | **4,757 ms** | 5,416 ms | **default, by 14%** |

Plus the record's own inversions: the builder's round-2 run (default 4,841 / personal 5,554 —
default by 15%), round 1's docs (personal by 36%), the round-1 closure (personal by 17%), and my
round-1 re-runs (a tie ×3). **Seven measurements, no stable ordering.** "A wash" is not a hedge here;
it is the strongest claim the data license.

This is [M7-R6](#m7-r6) → [M7-R10](#m7-r10) a third time — *a fix that reaches some of its siblings* —
and it inverts the usual direction, which is why it is worth a line: normally ROADMAP is stale and the
README over-claims. Here the **README is the accurate artifact** (round 1 said it would be) and the
ledger is the one advertising a number the evidence retired.

**Fix.**
- `ROADMAP.md:555`: state one figure for the shipped default and say the shapes are a wash, per the
  README. If a personal-shape number is wanted, it must carry the inversions beside it.
- `DEVLOG.md:226`: same. It is a historical entry, so the honest edit is a clause — *"…recorded here
  as 2.46 / 2.11; a later run inverted the ordering (4,841 / 5,554), and M7-R9 retired the
  comparison"* — rather than a silent renumber.
- While there: `ROADMAP.md:38`'s *"~1.9–2.3×"* band should read **~1.8–2.3×**; my contended run
  measured **1.81×**, below the stated floor.

**Test.** N/A (docs). Though note what *would* have caught it: PERF-M7-05 prints both shapes every
run, so a reader of its output cannot miss the inversion. The docs are the only place the stale
ordering survives, because they are the only place a number gets frozen.

> **Closed** (2026-07-17). The correction reached the two files it had missed — and then went further,
> because [M7-R12](#m7-r12) landed in the same round and the honest table turned out to be *no table*.
> `ROADMAP`'s S2 box and `DEVLOG`'s S2 no longer advertise 2.46 vs 2.11: they carry **~4.7 s** for the
> shipped default and say the two shapes are a **wash on latency**, which the data support better than
> I knew — my own isolated run has the default at 4,724 ms against the personal shape's 5,278, the
> *third* inversion across four runs.
>
> The finding's framing is the one to keep: **a correction that stops at the file where the error was
> noticed is half a correction.** The READMEs were fixed because the reviewer named them; ROADMAP and
> DEVLOG carried the same claim and were not named, so they survived. Fixing the instance instead of
> the claim is how one wrong number becomes three.

---

<a id="m7-r12"></a>
### M7-R12 — "power and thermal regime, nothing else" does not order the milestone's own data: battery beats three of four AC runs, and the ~2.5 s product claim is the best of six observations · measurement · **closed**

**Finding.** This is [M7-R9](#m7-r9)'s lesson arriving one level down, and R9 predicted it in its own
last paragraph: *"naming one variable does not make a measurement reproducible; it makes the un-named
ones harder to notice."* R9 named **AC**. AC does not identify the regime either.

`DEVLOG.md:239` states the model as settled: *"**2,462 / 3,943 / 4,933 ms**, three occasions, each
with a within-run spread under 7%. **Power and thermal regime, nothing else.**"*

**Every measurement of the shipped default now on record, mine included:**

| labelled regime | who / when | default (2×6) |
|---|---|---|
| "AC, full power" | builder, R1 closure | **2,462 ms** ← the README's headline |
| battery | reviewer, round 2 | 3,943 ms |
| **AC** (verified: `BatteryStatus=2`, 99%) | **me, today, isolated** | **4,757 ms** |
| "throttled AC" | builder, R2 closure | 4,841 ms |
| "AC" re-measure | builder, R2 closure | 4,933 ms |
| "AC", contended | me, today, documented invocation | 7,142 ms |

**The model is refuted by its own rows.** If AC-at-full-power is 2,462 and battery is 3,943, then
three *AC* runs at 4,757 / 4,841 / 4,933 are **slower than battery** — which no power model explains.
A variable that cannot sort the observations is not the variable. The third category ("throttled AC")
was introduced to absorb exactly this contradiction, and it was assigned **post hoc, from the number
itself**: no thermal state was ever measured. A run is labelled "throttled" because it was slow, and
then cited as evidence that slowness is throttling.

**And here is a variable that *is* measured, controlled, and large.** `m7_s2`, same box, same AC
state, ~40 minutes apart — the only difference is **how the harness is invoked**:

```text
documented invocation (`--test m7_latency -- --ignored --nocapture`; cargo runs all 5 perf tests
CONCURRENTLY — the run's own log shows m7_s1, m7_s1_throughput and m7_s2 all "running for over 60
seconds" together):
    pre-M7   12,945 ms      default  7,142 ms (1.81x)     personal  6,448 ms (2.01x)

isolated (`--test-threads=1 m7_s2`):
    pre-M7   10,106 ms      default  4,757 ms (2.12x)     personal  5,416 ms (1.87x)
```

**1.50× on the default's absolute, at constant power state, from the invocation alone** — and the
harness's own documented command is the *slow* one. That is comparable in size to the ~2× this
milestone made its headline lesson, and it is nobody's power governor.

**The consequence for the product claim.** `README.md:160-167` publishes **~2.5 s** as the per-turn
figure, labelled *"on AC power, best of 3"*. That is the **single fastest of four AC measurements**;
the other three cluster at **4,757 / 4,841 / 4,933**. The number a user reads is the outlier, and the
three observations that disagree with it were reclassified rather than averaged. **The honest AC
figure is ~4.8 s** — 1.9× the published one, and **60% over the ~3 s bar**.

**What this does and does not cost the stop decision — because the answer is "less than it looks".**
The stop **stands**, and I would defend it. The ~2× is real, it is the part that is about the code,
and I have now confirmed it against a fourth regime (1.81× / 2.12×) where the absolute moved 1.5×.
S3/S4 genuinely carry the risk the write-up says. **But it does not stand on "the bar is met."**
`ROADMAP.md:555`'s box is `[x]` with the headline *"Met on both shipped shapes"*, and across six
measurements by two people the bar was met **once**. The defensible sentence is not *"we met the bar,
so we stop"* — it is *"**we missed the bar; we are stopping anyway**, because the remaining gap is the
box and not the code, and because S3 buys it back at a risk we would rather price deliberately than
pay now."* That is a stronger position, and it is the one the evidence supports. The ROADMAP already
says most of it in the blockquote at `:560`; the checkbox headline above it says the opposite.

**Fix.**
- `DEVLOG.md:239`: strike *"nothing else"*. Record that the regime story is **partly** power and
  partly invocation (measured: 1.50× from test concurrency at constant power), and that **battery
  beat three AC runs**, so the ordering variable is not identified.
- `README.md:160-167` / `README.it.md`: publish **~4.8 s**, or a range (*"~2.5–4.9 s; typically ~4.8
  s on this box"*). One observation does not outrank the three that contradict it. The surrounding
  prose (*"expect roughly 2× worse when throttled"*, *"measure on your own box"*) is good and should
  stay — it is the headline that is cherry-picked.
- `ROADMAP.md:555`: the checkbox headline should say the bar is **missed at the shipped default on
  this box's typical AC state**, and that the stop rests on the **ratio**, not the bar. Fold in the
  blockquote's honesty rather than contradicting it two lines up.
- The harness should pin its own invocation: have `m7_s2`'s doc comment and TESTING's run recipe say
  **`--test-threads=1`**, since the documented command currently measures the product against four
  other copies of itself.

**Test.** The calibration leg *already* measures this and it is why the ratio survived — the guard is
sound, the **prose** is what outran the evidence. The narrow guard worth having: `m7_s2` should print
`pre_m7` in ms next to a recorded reference value, so the harness itself says *"this box is running
1.3× slower than the box the READMEs quote"* rather than leaving a reader to discover it in review
round 3. A calibration leg you print but never compare to anything is half a calibration.

> **Closed** (2026-07-17). **This is the finding that cost M7 its headline, and it should have.**
>
> **Re-measured under the conditions the finding prescribes** — isolated (`--test-threads=1`), on AC
> (`BatteryStatus=2`), with the new calibration leg reporting this box at **1.02× the reference**, i.e.
> demonstrably not a slow box:
>
> | | ms | vs pre-M7 |
> |---|---|---|
> | pre-M7 (2×1) | 10,351 | — |
> | **default (2×6)** | **4,724** | **2.19×** |
> | personal (1×12) | 5,278 | 1.96× |
>
> **4,724 ms — reproducing the reviewer's 4,757 independently.** So the honest figure is **~4.7 s**,
> and the **2,462 ms I published was the fastest of seven observations and has never reproduced.** The
> READMEs now carry ~4.7 s; ROADMAP's status row and S2 checkbox say the **bar was missed**; DEVLOG's
> S2 leads with it.
>
> **The worse half was the explanation, and the finding is exactly right about it.** I wrote *"power
> and thermal regime, nothing else"* over data where a **battery** run beat **three AC** runs. No power
> model orders that. And "throttled AC" was a category I invented **post hoc from the number itself** —
> a run was called slow *because it was slow*, then cited as evidence that slowness was throttling.
> **That is the circular move this entire milestone exists to name, committed inside the entry naming
> it.** Struck. The regime story now says the ordering variable is **not identified**, and names the
> one that is: **test concurrency, 1.50×** — cargo was running five benchmarks against each other under
> my own documented command. `--test-threads=1` is now in the harness doc, both run recipes, and
> TESTING's *Running* section.
>
> **The calibration leg now compares, per the finding's test note** (*"a calibration leg you print but
> never compare to anything is half a calibration"*): it prints this box against a recorded
> `REFERENCE_PRE_M7_MS`, so the harness itself says *"you are running 1.4× the reference"* instead of
> leaving that to review round 3.
>
> **And the stop decision is re-grounded rather than defended.** The reviewer's sentence is better than
> mine and is now the one in the ROADMAP: not *"we met the bar, so we stop"* but ***"we missed the bar;
> we are stopping anyway"*** — because what M7 could deliver it delivered (a reproducible ~2×), no
> arrangement of `pool × intra` closes 4.7 s to 3 s on this hardware, and **S3 is the right next lead
> precisely because it is indifferent to the box**. Buying its risk should be a deliberate act, not a
> consolation for missing a bar.

---

<a id="m7-r13"></a>
### M7-R13 — the ratio guard is vacuous at ≤2 cores, where the shipped default *is* `PRE_M7_SHAPE` — and it reports that as a regression · guard · **closed**

**Finding.** [M7-R9](#m7-r9)'s closure sells the ratio as universal: *"a ratio catches a real
regression in any regime and cannot go red because a laptop is unplugged"*, and `m7_latency.rs:684`
says it *"is checkable anywhere"*. The assert message (`:740-743`) goes further:

```text
"…Both legs ran in THIS run, so the box's power regime cancels — this is a real regression in the
 thread work, not a slow box."
```

**On a small box that sentence is false, and the guard cannot pass.** `PRE_M7_SHAPE` is `(2, 1)`.
Work `resolve_pool_and_intra(None, None, cores)` (verified with a probe against the real function):

```text
 cores  default(pool,intra)  personal(pool,intra)
     1  (2, 1)               (1, 1)    <-- IDENTICAL to PRE_M7_SHAPE: ratio == 1.0 by construction
     2  (2, 1)               (1, 2)    <-- IDENTICAL to PRE_M7_SHAPE: ratio == 1.0 by construction
     4  (2, 2)               (1, 4)
     6  (2, 3)               (1, 6)
    12  (2, 6)               (1, 12)   <-- the reference box
```

At `cores ≤ 2` the shipped default **is** the pre-M7 shape. The calibration leg and the shape under
test are the *same configuration*, the ratio is 1.0 up to noise, and the guard fails — announcing "a
real regression in the thread work" for a box where M7 changed nothing at all. At `cores = 4` the
default is `(2, 2)`: one thread more than pre-M7, against a **1.5× floor** on a curve the harness
itself measures as sublinear (`1×2` bought 1.67× on my isolated sweep, and that is the *generous*
`pool=1` leg). It is marginal at best.

**So the guard's floor is a function of the box's core count — the one variable it does not name.**
R9 traded an un-named *power* variable for an un-named *core-count* variable, in a guard whose whole
purpose is to be regime-free. Fourth instance of this milestone's signature move.

**It also states something true the milestone doesn't:** at `cores ≤ 2`, **M7 delivers nothing** for
the shipped default — the derivation returns the shape that already shipped. `ROADMAP.md:38` carries
*"~1.9–2.3× faster than pre-M7, regime-invariant"* unqualified. That is a claim about a 12-thread
box. It is not wrong on a 2-core VM; it is undefined there, and the honest statement is *the
improvement scales with the box, and is zero below 4 cores.*

**Fix.**
- **The guard:** skip or invert below the point where the derivation can differ. Cheapest honest
  version, at the top of `m7_s2`:
  ```rust
  let cores = available_cores();
  // Below 4 cores the derived default IS PRE_M7_SHAPE (2x1) — the calibration leg and the shape
  // under test are the same configuration, so the ratio is 1.0 by construction and means nothing.
  // M7 has nothing to deliver on such a box; say so rather than calling it a regression (M7-R13).
  if cores < 4 {
      eprintln!("SKIPPED: {cores} cores — the derived default is the pre-M7 shape here, so M7's \
                 ratio is 1.0 by construction and this guard cannot say anything.");
      return;
  }
  ```
  A `thread_tests` unit assert pinning `resolve_pool_and_intra(None, None, 2).1 == 1` — i.e. *the
  derivation is a no-op at 2 cores* — would make that domain a fact of the code rather than a
  comment.
- **The message** at `:740-743`: drop "*this is a real regression in the thread work, not a slow
  box*". It is a conclusion the assert has not earned; "not a slow box" is true, "not a small box" is
  not.
- **`ROADMAP.md:38`** and ARCHITECTURE → *NER threading*: state the domain — **the speedup scales
  with the core count and is zero below 4 cores**, which is the same domain-of-an-invariant point
  [M7-R4](#m7-r4) already promoted one paragraph up. They are the same lesson about the same
  derivation.

**Test.** This *is* the test; the change above is the fix. The unit half needs no model and belongs in
`thread_tests` beside `derives_the_documented_shapes`, which already pins `(1,12)`, `(2,12)`, `(4,12)`
— it simply never asks what happens when the box is small, which is exactly the regime the guard
breaks in.

> **Closed** (2026-07-17). Took the prescribed skip, and pinned the domain in the code rather than in a
> comment.
>
> `m7_s2` now returns early below `MIN_CORES_FOR_A_MEANINGFUL_RATIO` (4) and prints *why*: the derived
> default **is** `PRE_M7_SHAPE` there, so both legs are the same configuration and the ratio is 1.0 by
> construction. And `onnx::thread_tests::the_derivation_is_a_no_op_on_a_small_box` makes it a fact of
> the code — `resolve_pool_and_intra(None, None, {1,2,3})` all yield `(2, 1)`, and 4 cores is where the
> derivation first has something to say. The assert message dropped *"this is a real regression in the
> thread work, not a slow box"*: "not a slow box" is earned, "not a **small** box" was not — it now
> names the core count and points at this finding.
>
> **The claim inherited the domain too**, which is the part that outlives the guard: ROADMAP's status
> row said *"~1.9–2.3× faster than pre-M7, regime-invariant"* unqualified. That is a statement about a
> 12-thread box. It now reads **"scales with the box, zero below 4 cores"**.
>
> The reviewer's framing is the sting and it is fair: **R9 traded an un-named *power* variable for an
> un-named *core-count* variable, in a guard whose whole purpose is to be free of un-named variables.**
> Same lesson as [M7-R4](#m7-r4), one file over — *state an invariant with its domain, or it is false
> somewhere you haven't looked.*

---

<a id="m7-r14"></a>
### M7-R14 — both of the bar's constants are calibrated against runs nobody controlled: the 8 s ceiling nearly fires on the harness's own documented command, and the 1.5 floor keeps the 20% blindness the docs say the ratio cured · guard · **closed**

**Finding.** [M7-R9](#m7-r9) replaced one absolute constant with a ratio plus two new constants. The
ratio is right ([M7-R12](#m7-r12) confirms it survives a 1.5× regime shift). **Neither constant was
calibrated against anything but the builder's own uncontended runs.**

**1 — `ABSOLUTE_SANITY_CEILING_MS = 8_000` is not loose.** It is documented as *"deliberately far
above the ~3 s product bar"* and *"stays quiet through a 2× regime shift"* (`m7_latency.rs:494-502`).
Run the harness **the way its own doc comment says to** (`cargo test-onnx --test m7_latency --
--ignored --nocapture`, which runs all five perf tests concurrently) and on my box:

```text
default (NER_POOL_SIZE unset)  pool=2 intra=6   min 7142 ms   median 10391 ms
```

**Min 7,142 against a 8,000 ceiling — 11% headroom. The median, 10,391 ms, is 30% *over* it.** The
assert takes the min, so it squeaked through; a slightly slower box, or one more background process,
and it fires. And when it fires, its message says:

> **Check your box's power state first** — this ceiling is deliberately loose…

…which sends the reader after the wrong variable, because the power state is fine: the cause is that
cargo is running four other copies of the same benchmark. This is precisely the failure R9 diagnosed
in the 3 s bar — *"a red bar means the laptop is unplugged"* — reproduced at a different threshold,
with a message that hard-codes the misdiagnosis.

**2 — `MIN_SPEEDUP_VS_PRE_M7 = 1.5` keeps the blindness the docs say it cured.** `TESTING.md:530` and
`DEVLOG.md:254` both sell the ratio against the 3 s bar's two defects: it *"cannot go red because a
laptop is unplugged"* **and** the 3 s bar was *"blind to a genuine 20% regression (2,462 → 2,954)"*.
The ratio fixes the first. It does **not** fix the second. Every ratio observed, by both of us:

```text
1.81x (me, contended)  1.85x (AC)  1.87x (me, personal)  2.01x  2.10x  2.12x  2.26x (battery)
```

Worst observed: **1.81×**. Floor: **1.5**. So the guard tolerates a `1 - 1.5/1.81` = **17%
regression** — materially the same 20% it is advertised as catching. **1.5 is nonetheless the right
floor**, and I want that on the record so nobody "fixes" it into flakiness: the spread is 1.81–2.26,
so a 1.7 floor leaves 6% margin and would false-fire. The defect is the **claim**, not the number: a
ratio guard on a 1.81–2.26 spread buys regime-independence, not sensitivity, and the docs should say
which of R9's two complaints it actually answers.

**Fix.**
- Raise `ABSOLUTE_SANITY_CEILING_MS` to something genuinely order-of-magnitude (the win being guarded
  is 27 s → ~5 s, so **~15 s** still catches it with room), **or** pin the invocation to
  `--test-threads=1` so the constant means what it was calibrated to mean. Prefer both.
- Amend the ceiling's message: name **concurrent tests** alongside power state, since the documented
  command is the one that triggers it.
- `TESTING.md:526-533` / `DEVLOG.md:249-256`: say plainly that the ratio answers the **false
  positive** (a red bar from a slow box) and **not** the false negative (a ~20% regression still
  ships green), and that the floor is set by the observed 1.81–2.26 spread rather than by a
  sensitivity target. An honest guard states what it cannot see.

**Test.** The floor half needs no new test — it needs the sentence above. The ceiling half is pinned
by whatever the invocation ends up being: if `--test-threads=1` becomes the contract, the doc comment
and TESTING's recipe are the guard, and this finding is what says why they are load-bearing.

> **Closed** (2026-07-17). Both halves, and the second one is a sentence rather than a constant.
>
> **The ceiling: 8 s → 15 s, and `--test-threads=1` alongside it** — the finding's "prefer both", which
> is right because they fix different things. 8 s was calibrated against uncontended runs and was not
> loose: the reviewer's documented-command run hit a **median of 10,391 ms**, so the ceiling fired on
> the harness's own recipe *and blamed the reader's power state* for what was test concurrency. **A
> guard that fires on a correct build teaches people to ignore it.** The win being protected is
> 27 s → ~5 s, so 15 s still catches it with room. The message now names concurrent tests **first**,
> power state second — in that order, because that is the order of the evidence.
>
> **The floor stays at 1.5, and the docs now say what that buys.** Per the finding: 1.7 would leave 6%
> margin against the observed 1.81–2.26 spread and start false-firing, which is the failure the ratio
> was built to end. So the number is right and **the claim was wrong** — the ratio buys
> **regime-independence, not sensitivity**. It answers R9's false *positive* and not the false
> *negative*: a ~17% regression still ships green, materially the blindness the wall-clock bar had.
> That is now stated in the test's doc comment, TESTING's PERF-M7-05 entry, and DEVLOG's S2.
>
> **An honest guard states what it cannot see** — otherwise the next reader trusts it for a job it was
> never doing, which is how M7-R1 happened in the first place.

---

<a id="m7-r15"></a>
### M7-R15 — M7-R8's closure credits the chunked path with 60 entities that are 20 extra sentences · docs · **closed**

**Finding.** [M7-R8](#m7-r8)'s closure offers the entity count as proof the finding was real:

> The entity count moved **134 → 194** — which is itself the proof the finding was right: **those 60
> extra detections are the chunked path**, and they were never being compared before.

**They are not the chunked path. They are `repeat(40)` → `repeat(60)`.** Measured directly (probe
outside the repo, real model, `intra=1`, decomposing the guard's inputs):

```text
the 5 short inputs (unchanged by the fix):  14 entities
long_field = repeat(40):  120 entities   (40 sentences x 3)  ->  GUARD TOTAL = 134
long_field = repeat(60):  180 entities   (60 sentences x 3)  ->  GUARD TOTAL = 194
```

`SENTENCE` yields exactly **3** entities (Person + Organization + Location). 194 − 134 = 60 = **20
extra sentences × 3**. The delta is linear fixture growth and would be identical if chunking did not
exist. In fact the chunked path's contribution to the count is **exactly zero**: `repeat(60)` returns
`60 × 3 = 180`, i.e. the un-chunked linear count, with no boundary loss *and* no boundary
duplication — which is not guaranteed, and the sibling guard shows it: `onnx_ner_latency_and_recall_across_field_sizes`
reports **771/768** and **3084/3072**, so overlapping windows *can* double-count at a seam.

The finding was right and the fix is right — I verified both above. But **the evidence cited for it is
not evidence**: the proof that the guard now reaches `infer_chunked` is the **token count** (662 >
`MAX_WINDOW_TOKENS` = 480, and `chunk_char_ranges` returning 2 windows), which is exactly what the new
assert checks. The closure reached for the entity count instead — a number that moved for an unrelated
reason and happened to move at the same time.

This is [M7-R6](#m7-r6)'s standard applied to M7-R6's own family: *a number is verified against what
the test prints*. 194 **is** what the test prints. What it *means* was never checked, and it means
something else. That is the subtler failure, and it is the one this milestone keeps producing: R10 was
a rule applied past its evidence; this is a **conclusion attached to the nearest number that moved**.

**Fix.** `docs/reviews/M7.md` → M7-R8's closure note: replace the entity-count claim with the token
count, which is both true and the thing the assert now pins — *"the long input is 662 tokens against a
480-token window and `chunk_char_ranges` returns 2 windows, so the guard reaches `infer_chunked` for
the first time; the count moved 134 → 194 because the fixture grew by 20 sentences × 3 entities, and
the chunked path returned exactly the linear count — no seam loss, no seam duplication."* Keep the
verdict; drop the arithmetic that doesn't support it.

**Test.** N/A (a record correction). Worth noting what the corrected sentence buys: "the chunked path
returned exactly the linear count" is a **recall result about chunking** that nobody has stated, and
it is more interesting than the claim it replaces — the seam neither lost nor duplicated an entity on
this input, while the sibling guard shows seams *do* duplicate at larger sizes.

> **Closed** (2026-07-17). The closure note is corrected: **194 − 134 = 60 = the 20 extra sentences
> `repeat(60)` adds × 3 entities each.** The chunked path contributes **nothing** to the count — the
> proof that the guard now reaches `infer_chunked` is the **token count** the assert checks (662 > 480),
> which is exactly what M7-R8 was about.
>
> Small, and I want it on the record for what it says rather than what it costs: I attributed a number
> to the mechanism I wanted it to demonstrate, **inside the closure of a finding about doing precisely
> that**. The arithmetic was one subtraction away. *A number that agrees with you is the one to check
> hardest* — and I checked it least, because it agreed.

---

<a id="m7-r16"></a>
### M7-R16 — the byte-proxy assert M7-R8 killed is still alive 90 lines down, in the same file, guarding the same property · guard · **closed**

**Finding.** [M7-R8](#m7-r8) established a rule and TESTING now carries it: **assert the property the
code branches on, in the unit it branches on** — a byte count is a proxy, the tokenizer is the thing.
The fix applied it at `ner_perf.rs:246-257`. The identical pattern, with the identical comment, sits
untouched at `ner_perf.rs:334-339`:

```rust
let placeholders =
    "Contact [PERSON_1] at [ORG_1] in [LOCATION_1] about [EMAIL_1] and [PHONE_1]. ".repeat(40);
assert!(
    placeholders.len() > 2_000,
    "must be large enough to exercise the chunked path"      // <- bytes, for a token property
);
```

Same file, same claim ("*exercise the chunked path*"), same wrong unit — and this one guards
[M5-R4](M5.md#m5-r4), *placeholder inertness*, whose closure note is the one that says a **model swap
must re-check it**.

**It is true today — by luck, and the luck is worth quantifying.** Measured:

```text
repeat(40):   3080 bytes   1162 tokens  -> 3 window(s) CHUNKED
```

1,162 tokens against a 480-token window: comfortably chunked, ~2.4× margin. It is true because
placeholder-dense text tokenizes ~2.7× denser per byte than prose (`[`, `PERSON`, `_`, `1`, `]` each
cost a token), so the byte proxy over-shoots into correctness. **That is a property of the fixture's
character, not of the assert** — reword the placeholder string, drop the repeat count, or swap to a
tokenizer with a different vocabulary, and the assert keeps passing while the field stops chunking.
Exactly M7-R8's failure, with the sign flipped.

Low severity, and I want to be precise about why it is on the record at all rather than inflated: it
is **not** a live defect, the guard covers what it claims **today**, and I could not make it lie. It
is here because M7-R8's whole finding was that a green assert had stopped meaning what it said, and
the fix left its twin in the same file — so the *rule* landed in TESTING while the *instance* it was
written for survived. A rule that isn't applied to the cases it was derived from is a rule that will
be re-derived.

**Fix.** Same shape as M7-R8's, and everything needed is already imported into this file:

```rust
let placeholder_tokens = load_tokenizer()
    .encode(placeholders.as_str(), true).expect("encode").get_ids().len();
assert!(
    placeholder_tokens > MAX_WINDOW_TOKENS,
    "the placeholder field is {placeholder_tokens} tokens, not over the {MAX_WINDOW_TOKENS}-token \
     window — so this guard never reaches `infer_chunked`, and placeholder inertness goes unchecked \
     on the chunked path (M7-R8/M7-R16)"
);
```

**Test.** This *is* the test. Worth a sweep rather than a one-line fix: `> 2_000`-style byte asserts
standing in for token properties are now a known class in this repo (M5-R10, M7-R8, this), and
`ner_perf.rs` is the file that owns the token-count guards.

---

> **Closed** (2026-07-17). Applied M7-R8's own fix to its twin: `m5_r4_the_ner_treats_placeholders_as_inert`
> now runs the real tokenizer and asserts `placeholder_tokens > MAX_WINDOW_TOKENS` instead of
> `placeholders.len() > 2_000`.
>
> The finding is right that this was **not** a live defect — the field is 1,162 tokens, ~2.4× the
> window, so it does chunk today. It is right about *why it was true*, and that is the whole value:
> placeholder-dense text tokenizes ~2.7× denser per byte than prose (`[`, `PERSON`, `_`, `1`, `]` each
> cost a token), so the byte proxy **over-shot into correctness**. That is a property of the fixture's
> character, not of the assert — reword the string, lower the repeat, or swap the tokenizer's
> vocabulary and the assert keeps passing while the field stops chunking, taking M5-R4's placeholder
> inertness (*the property a model swap must re-check*) with it, silently.
>
> **The reviewer's closing line is the lesson, and it is aimed squarely at how I closed M7-R8:** *"a
> rule that isn't applied to the cases it was derived from is a rule that will be re-derived."* I put
> the rule in TESTING and left its twin alive ninety lines below the fix, in the same file, with the
> same comment. Both byte-proxy asserts in `ner_perf.rs` are now token asserts; there are no others.
